# 生产单连续报工功能方案

> 文档版本：v1.0  
> 适用模块：生产管理 — 连续报工  
> 依赖模块：工序任务管理、工序报工记录

---

## 一、需求背景

当前系统的报工流程为**逐条报工**：操作员需要先找到某一条工序任务，再填写报工数据。当一张生产单包含多道工序（如：炼胶 → 硫化 → 修边 → 包装）时，需要重复操作多次，效率较低。

**连续报工**旨在解决这个问题：操作员通过录入或扫描生产单编号，系统自动加载该生产单的所有工序任务，在一个页面上按工序顺序逐道（或批量）完成报工，实现**一单到底**的快速操作。

---

## 二、功能概述

### 核心能力

| 能力 | 说明 |
|------|------|
| 扫码/录入定位 | 扫描或手工输入生产单编号，一键加载所有工序任务 |
| 工序全览 | 按步骤顺序展示所有工序，显示计划数、已完工、剩余可报 |
| 连续报工 | 在同一页面依次为每道工序填写报工数据，无需切换页面 |
| 一键全工序报工 | 支持对所有未完成工序一次性填入相同的合格数量 |
| 实时校验 | 自动检查超产比例、审批状态、任务状态等 |
| 报工后自动刷新 | 每提交一道工序的报工后，自动刷新该工序的完成进度 |
| 历史记录 | 可展开查看每道工序的历史报工记录 |

### 操作场景

```
场景A：操作员在产线终端扫码报工
┌────────────────────────────────────┐
│  扫码枪扫描生产单条码              │
│         ↓                          │
│  页面自动加载该单所有工序           │
│         ↓                          │
│  填写班次、操作员（一次填写全局生效）│
│         ↓                          │
│  依次点击每道工序的"报工"按钮       │
│  或点击"全部报工"一键提交           │
└────────────────────────────────────┘

场景B：生产主管在办公室批量报工
┌────────────────────────────────────┐
│  手工输入生产单编号 → 查询          │
│         ↓                          │
│  检查各工序进度                     │
│         ↓                          │
│  调整每道工序的合格/不合格数量       │
│         ↓                          │
│  点击"全部报工"一次提交             │
└────────────────────────────────────┘
```

---

## 三、页面设计

### 3.1 页面布局

```
┌──────────────────────────────────────────────────────────┐
│  连续报工                                    [导出] [重置] │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌─ 生产单定位 ──────────────────────────────────────┐   │
│  │  [🔍 扫码或输入生产单编号____________] [查询]      │   │
│  └───────────────────────────────────────────────────┘   │
│                                                          │
│  ┌─ 生产单信息 ──────────────────────────────────────┐   │
│  │  生产单编号: PO-20250325-001                       │   │
│  │  产品编号: P001    产品名称: XX密封圈               │   │
│  │  规格: 50×30×2     计划数量: 10000                 │   │
│  │  设备: EQ-001      模具: M-001                     │   │
│  └───────────────────────────────────────────────────┘   │
│                                                          │
│  ┌─ 公共报工参数（全局生效）─────────────────────────┐   │
│  │  报工日期: [2025-03-25]  班次: [早班▼]             │   │
│  │  班组: [A组▼]  操作员: [张三▼]                     │   │
│  │  ─────────────────────────────────────             │   │
│  │  快捷操作:  填入数量 [______]  [应用到全部工序]     │   │
│  └───────────────────────────────────────────────────┘   │
│                                                          │
│  ┌─ 工序报工列表 ────────────────────────────────────┐   │
│  │                                                    │   │
│  │  工序1: 炼胶                                       │   │
│  │  ┌────────────────────────────────────────────┐   │   │
│  │  │ 状态: ● 进行中   审批: 已审批               │   │   │
│  │  │ 计划: 10000  已完成: 6000  剩余可报: 4000   │   │   │
│  │  │ ████████████████░░░░░░ 60%                 │   │   │
│  │  │                                             │   │   │
│  │  │ 合格数量: [______]  不合格: [______]         │   │   │
│  │  │ 不合格原因: [________________]               │   │   │
│  │  │                           [报工] [历史▼]    │   │   │
│  │  └────────────────────────────────────────────┘   │   │
│  │                                                    │   │
│  │  工序2: 硫化                                       │   │
│  │  ┌────────────────────────────────────────────┐   │   │
│  │  │ 状态: ● 未开始   审批: 已审批               │   │   │
│  │  │ 计划: 10000  已完成: 0  剩余可报: 10000     │   │   │
│  │  │ ░░░░░░░░░░░░░░░░░░░░░░ 0%                 │   │   │
│  │  │                                             │   │   │
│  │  │ 合格数量: [______]  不合格: [______]         │   │   │
│  │  │ 不合格原因: [________________]               │   │   │
│  │  │                           [报工] [历史▼]    │   │   │
│  │  └────────────────────────────────────────────┘   │   │
│  │                                                    │   │
│  │  工序3: 修边   ⚠ 未审批，不可报工                  │   │
│  │  ┌────────────────────────────────────────────┐   │   │
│  │  │ 状态: ● 未开始   审批: ⚠ 草稿（不可报工）   │   │   │
│  │  │ 计划: 10000  已完成: 0  剩余可报: -          │   │   │
│  │  │ 合格数量/不合格数量  (禁用状态)              │   │   │
│  │  └────────────────────────────────────────────┘   │   │
│  │                                                    │   │
│  │  工序4: 包装   ✅ 已完成                           │   │
│  │  ┌────────────────────────────────────────────┐   │   │
│  │  │ 状态: ✅ 已完成                             │   │   │
│  │  │ 计划: 10000  已完成: 10000                   │   │   │
│  │  │ ██████████████████████ 100%                │   │   │
│  │  └────────────────────────────────────────────┘   │   │
│  │                                                    │   │
│  └───────────────────────────────────────────────────┘   │
│                                                          │
│           [ 全部报工（提交 2 道工序）]                     │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

### 3.2 工序卡片状态

| 状态 | 颜色 | 可否报工 | 说明 |
|------|------|---------|------|
| 未开始 + 已审批 | 蓝色 | ✅ 可以 | 正常待报工 |
| 进行中 + 已审批 | 橙色 | ✅ 可以 | 已有部分报工 |
| 已完成 | 绿色 | ❌ 禁止 | 已报完，显示完成信息 |
| 已关闭 | 灰色 | ❌ 禁止 | 任务已关闭 |
| 未审批（草稿/待审批） | 黄色 | ❌ 禁止 | 提示"未审批不可报工" |

---

## 四、业务流程

### 4.1 主流程

```
用户扫码/输入生产单编号
        │
        ▼
┌──────────────────┐
│ 查询生产单信息    │ ← 验证生产单是否存在、是否已派发
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ 加载所有工序任务  │ ← 按 step_number 排序
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ 渲染工序卡片列表  │ ← 每道工序显示进度和输入框
└──────┬───────────┘
       │
       ▼ (用户填写报工数据)
       │
  ┌────┴────┐
  │         │
  ▼         ▼
单工序报工  全部报工
  │         │
  ▼         ▼
┌──────────────────┐
│ 前端校验          │ ← 数量范围、必填项
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ 调用后端快速报工   │ ← POST /work-reports/quick
│ (逐条提交)        │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ 自动刷新工序进度   │ ← 更新卡片进度条和数量
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ 继续下一道工序     │ ← 或全部完成
└──────────────────┘
```

### 4.2 扫码输入流程

```
扫码枪扫描条码 → 条码内容自动填入输入框
        │
        ▼
监听输入框的 @pressEnter 事件
        │
        ▼
自动触发查询（等同于点击"查询"按钮）
        │
        ▼
页面自动聚焦到第一道可报工工序的"合格数量"输入框
```

**扫码兼容说明：**
- 扫码枪本质上模拟键盘输入 + 回车，无需额外硬件对接
- 支持一维码（Code128/Code39）和二维码（QR Code）
- 条码内容即为生产单编号（如 `PO-20250325-001`）

### 4.3 "全部报工"批量提交流程

```
用户点击"全部报工"
        │
        ▼
┌──────────────────────────┐
│ 收集所有有效报工数据      │
│ 条件：                    │
│  - 合格数量 > 0           │
│  - 审批状态 = 已审批       │
│  - 任务状态 ≠ 已完成/已关闭│
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ 逐条按 step_number 顺序  │
│ 调用 POST /work-reports/quick │
│ (串行提交，保证顺序)      │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ 汇总提交结果              │
│ "成功报工 3 道工序，        │
│  失败 0 道"               │
└──────────────────────────┘
```

---

## 五、数据接口设计

### 5.1 需要使用的现有接口

| 接口 | 方法 | 用途 |
|------|------|------|
| `/api/orders` | GET | 根据编号查询生产单信息 |
| `/api/process-tasks` | GET | 根据 production_order_number 查询所有工序任务 |
| `/api/work-reports/quick` | POST | 快速报工（已有，无需新增） |
| `/api/work-reports` | GET | 查询历史报工记录 |
| `/api/schedules` | GET | 班次下拉数据 |
| `/api/groups` | GET | 班组下拉数据 |
| `/api/employees` | GET | 操作员下拉数据 |

### 5.2 需要新增的接口

**1. 按生产单获取工序任务及报工统计**

```
GET /api/process-tasks/by-order/:orderNo
```

**响应示例：**
```json
{
  "success": true,
  "data": {
    "order": {
      "production_order_number": "PO-20250325-001",
      "item_number": "P001",
      "item_name": "XX密封圈",
      "specifications": "50×30×2",
      "planned_quantity": 10000,
      "plan_status": "已派发",
      "equipment_number": "EQ-001",
      "mould_number": "M-001"
    },
    "tasks": [
      {
        "process_task_number": "PT-20250325-001",
        "step_number": 1,
        "standard_process_name": "炼胶",
        "planned_quantity": 10000,
        "completed_quantity": 6000,
        "remaining_quantity": 4000,
        "max_reportable": 4500,
        "excess_reporting_ratio": "5",
        "task_status": "进行中",
        "approval_status": "已审批",
        "work_center_name": "炼胶工作中心",
        "report_count": 3,
        "can_report": true
      },
      {
        "process_task_number": "PT-20250325-002",
        "step_number": 2,
        "standard_process_name": "硫化",
        "planned_quantity": 10000,
        "completed_quantity": 0,
        "remaining_quantity": 10000,
        "max_reportable": 10500,
        "task_status": "未开始",
        "approval_status": "已审批",
        "can_report": true
      }
    ]
  }
}
```

---

## 六、前端技术方案

### 6.1 组件结构

```
ContinuousReport.vue (主页面)
├── 扫码/输入区域
│   └── a-input-search（支持 pressEnter 扫码触发）
├── 生产单信息卡片
│   └── a-descriptions（展示生产单基本信息）
├── 公共参数区域
│   ├── a-date-picker（报工日期）
│   ├── a-select（班次）
│   ├── a-select（班组）
│   ├── a-select（操作员）
│   └── 快捷操作：数量输入 + 应用到全部
├── 工序卡片列表
│   └── v-for="task in tasks"
│       ├── 工序头部（名称、状态标签、进度条）
│       ├── 数据区（计划/已完成/剩余）
│       ├── 输入区（合格数量、不合格数量、原因）
│       ├── 操作按钮（单工序报工、查看历史）
│       └── 历史报工折叠区（a-collapse）
└── 底部汇总操作栏
    └── a-button（全部报工，显示待提交数量）
```

### 6.2 核心状态管理

```typescript
// 生产单信息
const orderInfo = ref<any>(null)

// 工序任务列表
const tasks = ref<TaskWithReport[]>([])

// 公共报工参数
const commonParams = reactive({
  report_date: dayjs().format('YYYY-MM-DD'),
  schedules_id: '',
  schedules_name: '',
  group_number: '',
  group_name: '',
  operator_number: '',
  operator_name: ''
})

// 每道工序的报工输入
interface TaskWithReport {
  // 任务原始数据
  process_task_number: string
  step_number: number
  standard_process_name: string
  planned_quantity: number
  completed_quantity: number
  remaining_quantity: number
  max_reportable: number
  task_status: string
  approval_status: string
  can_report: boolean
  // 报工输入
  input_qualified_qty: string    // 用户输入的合格数量
  input_unqualified_qty: string  // 用户输入的不合格数量
  input_unqualified_reason: string
  input_remark: string
  // 状态
  submitting: boolean
  submitted: boolean
}
```

### 6.3 扫码实现

```typescript
// 扫码输入框
const orderInput = ref('')
const inputRef = ref<HTMLInputElement>()

// 扫码枪模拟键盘输入 + Enter，直接触发查询
const handleScanOrSearch = () => {
  if (!orderInput.value.trim()) return
  loadOrderAndTasks(orderInput.value.trim())
}

// 页面加载后自动聚焦输入框，方便直接扫码
onMounted(() => {
  nextTick(() => inputRef.value?.focus())
})
```

### 6.4 报工校验规则

```typescript
const validateReport = (task: TaskWithReport): string | null => {
  const qty = parseFloat(task.input_qualified_qty) || 0
  const unqty = parseFloat(task.input_unqualified_qty) || 0

  if (qty <= 0) return '合格数量必须大于0'
  if (qty > task.max_reportable) {
    return `合格数量(${qty})超过最大可报数量(${task.max_reportable})`
  }
  if (unqty > 0 && !task.input_unqualified_reason.trim()) {
    return '有不合格数量时必须填写不合格原因'
  }
  if (!commonParams.report_date) return '请选择报工日期'
  if (!commonParams.operator_number) return '请选择操作员'
  return null // 校验通过
}
```

---

## 七、后端新增接口详细设计

### 7.1 按生产单获取工序及报工统计

**路由：** `GET /api/process-tasks/by-order/:orderNo`

**Controller 逻辑：**

```typescript
// 1. 查询生产单基本信息
SELECT production_order_number, production_number, item_number, item_name,
       specifications, basic_unit, planned_quantity, plan_status,
       equipment_number, equipment_name, mould_number, production_date, schedule_id
FROM production_order
WHERE production_order_number = :orderNo

// 2. 查询该单所有工序任务
SELECT process_task_number, step_number, standard_process_name,
       item_number, item_name, specifications, basic_unit,
       work_center_number, work_center_name,
       planned_quantity, completed_quantity, excess_reporting_ratio,
       task_status, approval_status
FROM process_task
WHERE production_order_number = :orderNo
ORDER BY step_number ASC

// 3. 统计每道工序的报工次数
SELECT process_task_number, COUNT(*) as report_count
FROM work_report
WHERE production_order_number = :orderNo
GROUP BY process_task_number

// 4. 计算 remaining_quantity, max_reportable, can_report
// remaining = planned - completed
// max_reportable = planned × (1 + excess_ratio/100) - completed
// can_report = approval_status='已审批' AND task_status NOT IN ('已完成','已关闭')
```

---

## 八、与现有系统的关系

### 8.1 复用现有能力

| 组件 | 复用方式 |
|------|---------|
| `POST /work-reports/quick` | 直接调用，每道工序独立提交一条报工记录 |
| 数量校验逻辑 | 后端已有超产比例校验，无需修改 |
| 审批状态检查 | 后端已有 approval_status 校验，无需修改 |
| 任务状态自动流转 | 已有 onWorkReportApproved 回调，自动更新 completed_quantity 和 task_status |
| work_report 表 | 完全复用，无需改表 |
| process_task 表 | 完全复用，无需改表 |

### 8.2 不影响现有功能

- 现有的**工序报工记录页面**（逐条报工）继续可用
- 现有的**移动端报工页面**继续可用
- 新增的连续报工只是提供了一个更高效的操作入口
- 报工数据最终存储在同一张 work_report 表中，结构完全一致

---

## 九、页面入口

### 9.1 菜单位置

在"生产管理"子菜单中增加"连续报工"菜单项：

```
├── 🔧 生产管理
│   ├── 📋 工序任务管理
│   ├── 📦 生产单备料
│   ├── 📝 工序报工记录
│   └── ⚡ 连续报工          ← 新增
```

### 9.2 路由

```typescript
{
  path: 'continuous-report',
  name: 'ContinuousReport',
  component: () => import('@/views/WorkReport/ContinuousReport.vue'),
  meta: { title: '连续报工' }
}
```

---

## 十、扩展能力

### 10.1 条码方案

| 条码类型 | 内容 | 适用场景 |
|---------|------|---------|
| 生产单条码 | `PO-20250325-001` | 贴在生产工票上，一单一码 |
| 工序任务条码 | `PT-20250325-001` | 贴在工序流转卡上，一道工序一码 |
| 复合条码 | `PO-20250325-001|EQ-001|M-001` | 包含设备和模具信息（扩展用） |

当前方案以**生产单条码**为主，扫码即加载该单全部工序。

### 10.2 后续可扩展功能

| 功能 | 说明 |
|------|------|
| 扫码自动填入操作员 | 操作员工牌扫码后自动识别员工 |
| 设备/模具条码绑定 | 扫码自动关联设备和模具编号 |
| 报工审批工作流 | 连续报工后进入审批流，主管确认后生效 |
| 异常报工预警 | 合格率过低时自动弹出预警 |
| 打印报工回执 | 报工完成后可打印包含二维码的回执 |

---

## 十一、源码规划

| 文件 | 说明 |
|------|------|
| `server/src/controllers/processTask.controller.ts` | 新增 `getTasksByOrder` 函数 |
| `server/src/routes/processTask.routes.ts` | 新增 `GET /by-order/:orderNo` 路由 |
| `client/src/views/WorkReport/ContinuousReport.vue` | 连续报工主页面 |
| `client/src/api/workReport.ts` | 新增 `getTasksByOrder()` API |
| `client/src/router/index.ts` | 新增路由 |
| `client/src/components/Layout/AppSidebar.vue` | 新增菜单项 |
