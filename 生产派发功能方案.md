# 生产派发功能方案

> 文档版本：v1.0  
> 适用模块：生产单管理 — 未开始页签  
> 涉及源码：`client/src/views/Order/List.vue`、`server/src/controllers/order.controller.ts`

---

## 一、功能概述

生产派发是将处于"未开始"状态的生产单分配到具体设备、模具和班次的核心操作。系统采用**一键派发**模式，在完成派发的同时自动生成对应的**工序任务**和**备料单**，实现从计划到执行的一体化衔接。

### 核心能力一览

| 能力 | 说明 |
|------|------|
| 批量派发 | 支持同时选择多条生产单进行派发 |
| 资源分配 | 为每条生产单指定设备、模具、生产日期和班次 |
| 批量修改 | 批量设置设备/模具/日期/班次并应用到勾选行 |
| 排产冲突检查 | 自动校验模具和设备在相同日期班次是否冲突 |
| 自动生成工序任务 | 根据工艺路线自动创建工序任务单 |
| 自动生成备料单 | 根据BOM自动创建备料单及明细 |
| 事务保护 | 全部操作在同一数据库事务中执行，失败自动回滚 |

---

## 二、业务流程

```
用户选择生产单 → 打开派发对话框 → 填写派发信息 → 确定派发
                                                      │
                    ┌─────────────────────────────────┘
                    ▼
            ┌──────────────┐
            │ 前端预检      │ ← 本批次内模具/设备冲突检查
            │ (即时反馈)    │
            └──────┬───────┘
                   │ 无冲突
                   ▼
            ┌──────────────┐
            │ 后端校验      │ ← 数据库已派发记录冲突检查
            │ (HTTP请求)    │
            └──────┬───────┘
                   │ 无冲突
                   ▼
        ┌──────────────────────┐
        │ 事务处理（逐条执行）    │
        │                      │
        │  第一步：验证并派发     │ ← 检查审批状态、计划状态
        │  第二步：生成工序任务   │ ← 匹配工艺路线 → 创建任务
        │  第三步：生成备料单     │ ← 匹配BOM → 展平 → 创建备料
        │                      │
        └──────────┬───────────┘
                   │
                   ▼
            ┌──────────────┐
            │ 返回结果汇总   │ → 派发数、任务数、备料单数
            └──────────────┘
```

---

## 三、前端操作界面

### 3.1 前置条件检查

用户在"未开始"页签选择记录后点击"派发"按钮，系统首先进行前置校验：

| 检查项 | 条件 | 不满足时提示 |
|--------|------|-------------|
| 选择记录 | `selectedRowKeys.length > 0` | "请先选择要派发的记录" |
| 计划状态 | `plan_status === '未开始'` | "只能派发状态为'未开始'的记录，请重新选择" |
| 审批状态 | `approval_status === '已审批'` | "只能派发审批状态为'已审批'的记录，请先完成审批" |

### 3.2 派发对话框

通过校验后弹出派发对话框，包含以下区域：

#### 批量修改区域

| 字段 | 输入方式 | 说明 |
|------|---------|------|
| 设备编号 | 远程搜索（AutoComplete） | 输入关键词搜索设备，选中后自动填入设备编号和名称 |
| 模具号 | 远程搜索（AutoComplete） | 输入关键词搜索模具，选中后自动填入模具号及相关参数 |
| 生产日期 | 日期选择器（DatePicker） | 选择排产日期 |
| 班次 | 下拉选择（Select） | 从班次基础数据中选择 |

点击"批量应用"按钮后，将上述字段值应用到所有勾选的行。

#### 派发明细表格

每条生产单显示一行，可逐行编辑：

| 列名 | 来源 | 可编辑 |
|------|------|--------|
| 生产单编号 | 生产单主数据 | 否 |
| 生产计划编号 | 生产单主数据 | 否 |
| 产品编号 | 生产单主数据 | 否 |
| 产品名称 | 生产单主数据 | 否 |
| 规格 | 生产单主数据 | 否 |
| 计划数量 | 生产单主数据 | 否 |
| 设备编号 | 用户填写 | **是**（远程搜索） |
| 模具号 | 用户填写 | **是**（远程搜索） |
| 成型件规格 | 模具自动带入 | 否（跟随模具） |
| 成型件单耗 | 模具自动带入 | 否（跟随模具） |
| 实际模腔数 | 模具自动带入 | 否（跟随模具） |
| 实际穴数 | 模具自动带入 | 否（跟随模具） |
| 实际班产 | 模具自动带入 | 否（跟随模具） |
| 生产日期 | 用户填写 | **是**（日期选择） |
| 班次 | 用户填写 | **是**（下拉选择） |

#### 模具选择自动带入规则

选中模具后，系统自动从模具主数据中带入以下字段：

```
模具号 (item_number)               → mould_number
成型件规格 (formed_part_specifications)  → formed_part_specifications
成型件物料消耗 (formed_part_materia_consumption) → formed_part_unit_consumption
实际运行频率 (actual_operation_frequency)  → actual_cavity_count
实际模腔数 (actual_cavities_number)     → actual_hole_count
实际产量 (actual_production_number)     → actual_daily_output
```

---

## 四、排产冲突检查机制

系统实施**前端预检 + 后端校验**的双层防护策略，确保同一台设备或同一幅模具在同一天同一班次不会被重复分配。

### 4.1 冲突检查规则

| 冲突类型 | 组合键 | 说明 |
|---------|--------|------|
| 模具冲突 | `模具号 + 生产日期 + 班次` | 同一幅模具在同一天同一班次只能分配给一条生产单 |
| 设备冲突 | `设备编号 + 生产日期 + 班次` | 同一台设备在同一天同一班次只能分配给一条生产单 |

### 4.2 前端预检（即时反馈）

在用户点击"确定派发"时，前端**立即**检查本批次内的冲突，无需等待服务器响应：

**检查范围：** 仅限当前批次内的所有派发行

**检查逻辑：**
1. 遍历所有派发行，按 `模具号|生产日期|班次` 分组，若同一组合出现在多条生产单中则判定为模具冲突
2. 遍历所有派发行，按 `设备编号|生产日期|班次` 分组，若同一组合出现在多条生产单中则判定为设备冲突

**冲突时：** 弹出红色错误弹窗，列出所有冲突详情，**阻止提交**

### 4.3 后端校验（数据库级别）

前端预检通过后，请求发送到服务器，后端进行4层校验：

| 层级 | 检查内容 | 检查范围 | HTTP状态码 |
|------|---------|---------|-----------|
| 第1层 | 模具+日期+班次 重复 | 本批次内 | 400 |
| 第2层 | 设备+日期+班次 重复 | 本批次内 | 400 |
| 第3层 | 模具+日期+班次 已占用 | 数据库已派发记录 | 409 |
| 第4层 | 设备+日期+班次 已占用 | 数据库已派发记录 | 409 |

**数据库查询逻辑（第3、4层）：**

```sql
-- 模具冲突检查示例
SELECT production_order_number, mould_number, production_date, schedule_id
FROM production_order
WHERE plan_status != N'未开始'                          -- 仅检查已派发的记录
  AND production_order_number NOT IN (:本批次单号列表)    -- 排除本批次自身
  AND (
    (mould_number = :模具号1 AND production_date = :日期1 AND schedule_id = :班次1)
    OR (mould_number = :模具号2 AND production_date = :日期2 AND schedule_id = :班次2)
    ...
  )
```

**冲突时响应格式：**

```json
{
  "success": false,
  "message": "排产冲突，派发失败",
  "conflicts": [
    "模具 M-001 在 2025-03-25 早班 已被生产单 PO-20250325-001 占用",
    "设备 EQ-002 在 2025-03-25 早班 已被生产单 PO-20250325-003 占用"
  ]
}
```

### 4.4 冲突提示界面

冲突信息以**模态弹窗**形式展示，每条冲突独立一行，左侧带红色标识线，方便用户快速定位问题：

```
┌─────────────────────────────────────────────┐
│  ✕  排产冲突，派发失败                         │
│                                             │
│  以下资源在相同日期和班次已有排产记录：           │
│                                             │
│  ▌模具 M-001 在 2025-03-25 早班              │
│  ▌  已被生产单 PO-20250325-001 占用           │
│                                             │
│  ▌设备 EQ-002 在 2025-03-25 早班              │
│  ▌  已被生产单 PO-20250325-003 占用           │
│                                             │
│                              [ 知道了 ]      │
└─────────────────────────────────────────────┘
```

---

## 五、后端处理流程

### 5.1 API接口

| 项目 | 值 |
|------|-----|
| URL | `POST /api/orders/dispatch-and-generate` |
| 认证 | JWT Bearer Token（authenticate 中间件） |
| Content-Type | application/json |

### 5.2 请求参数

```json
{
  "items": [
    {
      "production_order_number": "PO-20250325-001",
      "equipment_number": "EQ-001",
      "equipment_name": "1号硫化机",
      "mould_number": "M-001",
      "formed_part_specifications": "50x30x2",
      "formed_part_unit_consumption": "0.15",
      "actual_cavity_count": "4",
      "actual_hole_count": "8",
      "actual_daily_output": "1200",
      "production_date": "2025-03-25",
      "schedule_id": "早班"
    }
  ]
}
```

### 5.3 三步处理详情

#### 第一步：验证并派发

**前置验证：**

| 验证项 | 条件 | 失败处理 |
|--------|------|---------|
| 生产单存在 | 通过生产单编号查询记录存在 | 记录错误，跳过该条 |
| 审批状态 | `approval_status = '已审批'` | 记录错误，跳过该条 |
| 计划状态 | `plan_status = '未开始'` | 记录错误，跳过该条 |

**派发执行：**

将生产单状态更新为"已派发"，同时写入设备、模具等排产信息：

```sql
UPDATE production_order SET
  plan_status = N'已派发',
  equipment_number = :设备编号,
  equipment_name = :设备名称,
  mould_number = :模具号,
  formed_part_specifications = :成型件规格,
  formed_part_unit_consumption = :成型件单耗,
  actual_cavity_count = :实际模腔数,
  actual_hole_count = :实际穴数,
  actual_daily_output = :实际班产,
  production_date = :生产日期,
  schedule_id = :班次
WHERE production_order_number = :生产单编号
```

#### 第二步：自动生成工序任务

**匹配逻辑：**
1. 根据生产单的**产品编号**（item_number）匹配工艺路线（routing_header）
2. 匹配条件：`condition = '启用'` 且 `approval_status = '已审批'`
3. 取最新创建的一条路线（ORDER BY creation_date DESC, TOP 1）
4. 读取路线的所有工序明细（routing_detail），按 step_number 排序

**生成规则：**
- 每个工序步骤生成一条工序任务
- 任务编号格式：`PT-YYYYMMDD-NNN`（当日自增序号，3位补零）
- 如果已存在相同 `生产单号 + 步骤号` 的任务则跳过（幂等处理）
- 初始状态：`task_status = '未开始'`，`approval_status = '草稿'`

**工序任务字段映射：**

| 工序任务字段 | 数据来源 |
|------------|---------|
| process_task_number | 系统自动生成 |
| production_order_number | 生产单编号 |
| production_number | 生产计划编号 |
| process_route_number | 匹配到的工艺路线编号 |
| step_number | 工艺路线明细的步骤号 |
| item_number / item_name | 生产单产品信息 |
| specifications / basic_unit | 生产单产品规格/单位 |
| planned_quantity | 生产单计划数量 |
| standard_process_number/name | 工艺路线明细的标准工序 |
| work_center_number/name | 工艺路线明细的工作中心 |
| process_material_input_* | 工艺路线明细的物料投入信息 |

**跳过场景：**
- 未找到匹配的工艺路线
- 工艺路线无明细数据
- 该步骤的任务已存在

#### 第三步：自动生成备料单

**匹配逻辑：**
1. 根据产品编号匹配BOM（bom_header）
2. 匹配条件：`condition = '启用'` 且 `approval_status = '已审批'`
3. 取最新版本的一条BOM（ORDER BY bom_version DESC, TOP 1）
4. 递归展平BOM（最大深度10层，防止循环引用）
5. 合并相同物料（按 material_number 汇总数量）

**需求量计算公式：**

```
倍率 = 计划数量 / BOM基础数量
需求量 = 倍率 × 累计数量（四舍五入保留4位小数）
```

**生成内容：**
- **备料单主表**（material_preparation）：1条，编号格式 `MP-YYYYMMDD-NNN`
- **备料单明细**（material_preparation_detail）：每种物料1条

**备料单明细字段映射：**

| 字段 | 来源 |
|------|------|
| material_number / material_name | BOM展平后的物料信息 |
| required_quantity / adjusted_quantity | 按倍率计算的需求量 |
| bom_standard_quantity | BOM明细的标准用量 |
| bom_wastage_rate | BOM明细的损耗率 |
| step_number | BOM明细关联的工序步骤 |
| work_center_number/name | 工序任务中对应步骤的工作中心 |
| is_key_material | BOM明细标识 |
| bom_path | 展平路径（多级BOM可追溯） |

**跳过场景：**
- 该生产单已存在备料单
- 未找到匹配的BOM
- BOM展平后无物料明细

### 5.4 事务与错误处理

| 机制 | 说明 |
|------|------|
| 数据库事务 | 全部操作（派发+工序任务+备料单）在同一个 `sequelize.transaction()` 中执行 |
| 原子性 | 任何一环出现数据库异常，整个事务回滚，所有变更撤销 |
| 非致命错误 | 个别生产单不符合条件（如未审批）时记录错误并跳过，不影响其他单据 |
| 幂等设计 | 工序任务和备料单在插入前检查是否已存在，避免重复生成 |

### 5.5 成功响应

```json
{
  "success": true,
  "message": "成功派发 3 条生产单，生成 12 条工序任务，生成 3 份备料单",
  "data": {
    "dispatch": {
      "count": 3,
      "items": [
        { "orderNo": "PO-20250325-001", "status": "dispatched" },
        { "orderNo": "PO-20250325-002", "status": "dispatched" },
        { "orderNo": "PO-20250325-003", "status": "dispatched" }
      ]
    },
    "processTasks": {
      "totalGenerated": 12,
      "details": [
        { "orderNo": "PO-20250325-001", "tasksGenerated": 4 },
        { "orderNo": "PO-20250325-002", "tasksGenerated": 4 },
        { "orderNo": "PO-20250325-003", "tasksGenerated": 4 }
      ]
    },
    "materialPreparations": {
      "totalGenerated": 3,
      "details": [
        { "orderNo": "PO-20250325-001", "materialsGenerated": 8 },
        { "orderNo": "PO-20250325-002", "materialsGenerated": 6 },
        { "orderNo": "PO-20250325-003", "materialsGenerated": 10 }
      ]
    },
    "errors": []
  }
}
```

---

## 六、数据模型

### 6.1 涉及的数据库表

| 表名 | 角色 | 操作 |
|------|------|------|
| production_order | 生产单主表 | UPDATE（派发信息） |
| routing_header | 工艺路线头表 | SELECT（匹配路线） |
| routing_detail | 工艺路线明细表 | SELECT（读取工序步骤） |
| process_task | 工序任务表 | INSERT（自动生成） |
| bom_header | BOM头表 | SELECT（匹配BOM） |
| bom_detail | BOM明细表 | SELECT（物料清单） |
| material_preparation | 备料单主表 | INSERT（自动生成） |
| material_preparation_detail | 备料单明细表 | INSERT（自动生成） |

### 6.2 生产单派发相关字段

```
production_order
├── production_order_number  (PK)    -- 生产单编号
├── production_number               -- 生产计划编号
├── item_number                     -- 产品编号
├── item_name                       -- 产品名称
├── specifications                  -- 规格
├── basic_unit                      -- 单位
├── planned_quantity                -- 计划数量
├── approval_status                 -- 审批状态（已审批/草稿/...）
├── plan_status                     -- 计划状态（未开始/已派发/...）
│
│   ┌── 以下字段在派发时写入 ──┐
├── equipment_number                -- 设备编号
├── equipment_name                  -- 设备名称
├── mould_number                    -- 模具号
├── formed_part_specifications      -- 成型件规格
├── formed_part_unit_consumption    -- 成型件单耗
├── actual_cavity_count             -- 实际模腔数
├── actual_hole_count               -- 实际穴数
├── actual_daily_output             -- 实际班产
├── production_date                 -- 生产日期
└── schedule_id                     -- 班次
```

---

## 七、编号生成规则

| 类型 | 前缀 | 格式 | 示例 |
|------|------|------|------|
| 工序任务编号 | PT | `PT-YYYYMMDD-NNN` | PT-20250325-001 |
| 备料单编号 | MP | `MP-YYYYMMDD-NNN` | MP-20250325-001 |

**生成逻辑：**
- 查询当日已有编号的最大序号
- 新序号 = 最大序号 + 1
- 序号不足3位补零
- 在事务内查询，保证并发安全

---

## 八、技术实现要点

### 8.1 前端技术栈

| 技术 | 用途 |
|------|------|
| Vue 3 Composition API | 组件逻辑组织 |
| Ant Design Vue | UI组件（Modal、Table、AutoComplete、DatePicker、Select） |
| createVNode | 动态构建冲突提示弹窗的富文本内容 |
| computed | 派发选中数量计算、设备/模具搜索选项映射 |
| ref | 派发行数据、加载状态、对话框可见性管理 |

### 8.2 后端技术栈

| 技术 | 用途 |
|------|------|
| Express.js | HTTP路由和中间件 |
| Sequelize | 数据库ORM（事务管理、参数化查询） |
| SQL Server | 数据库（兼容 SQL Server 2008+） |
| JWT | 用户身份认证 |

### 8.3 安全措施

| 措施 | 说明 |
|------|------|
| 参数化查询 | 所有SQL使用 `:param` 占位符，防止SQL注入 |
| 身份认证 | 接口需要JWT Token，操作记录创建人 |
| 前端预校验 | 状态、审批等基本条件在前端先行过滤 |
| 后端二次校验 | 后端在事务内再次校验状态，防止并发问题 |
| 冲突双重检查 | 前端即时预检 + 后端数据库级别检查 |

---

## 九、异常场景处理

| 场景 | 系统行为 |
|------|---------|
| 未选择记录就点派发 | 前端提示"请先选择要派发的记录" |
| 选中未审批的记录 | 前端提示"只能派发审批状态为'已审批'的记录" |
| 本批次内模具冲突 | 前端弹窗列出所有冲突详情，阻止提交 |
| 本批次内设备冲突 | 前端弹窗列出所有冲突详情，阻止提交 |
| 数据库已有模具排产冲突 | 后端返回409，前端弹窗列出冲突记录 |
| 数据库已有设备排产冲突 | 后端返回409，前端弹窗列出冲突记录 |
| 并发操作导致状态已变更 | 后端在事务内再次校验状态，跳过该条并记录错误 |
| 找不到工艺路线 | 派发照常执行，工序任务跳过并在结果中说明原因 |
| 找不到BOM | 派发照常执行，备料单跳过并在结果中说明原因 |
| 数据库异常 | 整个事务回滚，所有变更撤销，返回500错误 |
| 网络超时 | 前端显示"派发操作失败"通用提示 |

---

## 十、源码位置索引

| 功能模块 | 文件 | 起始行号 |
|---------|------|---------|
| 前端：派发数据结构定义 | `client/src/views/Order/List.vue` | 822 |
| 前端：设备/模具远程搜索 | `client/src/views/Order/List.vue` | 869 |
| 前端：打开派发对话框 | `client/src/views/Order/List.vue` | 1000 |
| 前端：批量应用到勾选行 | `client/src/views/Order/List.vue` | 1068 |
| 前端：排产冲突预检+确定派发 | `client/src/views/Order/List.vue` | 1131 |
| 前端：API调用封装 | `client/src/api/order.ts` | 47 |
| 后端：路由定义 | `server/src/routes/order.routes.ts` | 16 |
| 后端：排产冲突检查 | `server/src/controllers/order.controller.ts` | 931 |
| 后端：验证并派发 | `server/src/controllers/order.controller.ts` | 1055 |
| 后端：自动生成工序任务 | `server/src/controllers/order.controller.ts` | 1109 |
| 后端：自动生成备料单 | `server/src/controllers/order.controller.ts` | 1201 |
| 后端：事务提交与结果汇总 | `server/src/controllers/order.controller.ts` | 1346 |
