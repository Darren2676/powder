# 采购对账功能迁移方案（无多工厂版）

> **适用场景**：从塑粉 MOM 系统迁移"采购对账"功能到另一个**没有多工厂隔离**的同技术栈系统。
> **技术栈要求**：Vue 3 + Ant Design Vue（前端） / Node.js + Express + Sequelize + SQL Server（后端）

---

## 一、功能概述

采购对账在入库单明细行（`stock_in_detail`）和采购订单明细行（`purchase_order_detail`）上操作，分为两个 Tab：

| Tab | 数据来源 | 核心操作 |
|-----|----------|----------|
| **入库单明细** | `stock_in_detail` JOIN `stock_in` | 查询 / 标记对账 / 打印对账单 |
| **采购订单明细** | `purchase_order_detail` JOIN `purchase_order` | 查询 / 标记对账 / 打印对账单 |

**锁定规则**：
- 入库单 Tab：`stock_in_status = 已审批 && reconciliation_status = 已对账 && invoice_status = 已开票` → 锁定
- 采购订单 Tab：`approval_status = 已审批 && reconciliation_status = 已对账 && invoice_status = 已开票` → 锁定

---

## 二、完整文件清单（共 13 处）

| # | 层级 | 文件 | 操作 |
|---|------|------|------|
| 1 | 数据库 | `stock_in_detail` 表 | 执行 SQL 加列 |
| 2 | 数据库 | `purchase_order_detail` 表 | 执行 SQL 加列 |
| 3 | 后端 | `stockIn.controller.ts` | 追加 3 个函数（已去工厂） |
| 4 | 后端 | `purchaseOrder.controller.ts` | 追加 3 个函数（已去工厂） |
| 5 | 后端 | `stockIn.routes.ts` | 追加 3 条路由 |
| 6 | 后端 | `purchaseOrder.routes.ts` | 追加 3 条路由 |
| 7 | 后端 | 菜单权限表 | 插入菜单记录 |
| 8 | 前端 | `views/purchasing/PurchaseReconciliation/Index.vue` | 复制并删工厂相关 |
| 9 | 前端 | `api/purchasing/purchaseReconciliation.ts` | 复制整个文件 |
| 10 | 前端 | `router/purchasing.ts` | 追加路由 + import |

---

## 三、依赖预检

目标系统**必须具备**以下表和功能：

| 依赖项 | 类型 | 说明 |
|--------|------|------|
| `stock_in` 表 | 数据库 | 入库单主表（含 `supplier_name`, `stock_in_date`, `factory_id` 等） |
| `stock_in_detail` 表 | 数据库 | 入库单明细表（含 `item_number`, `quantity`, `purchase_order_number` 等） |
| `purchase_order` 表 | 数据库 | 采购订单主表（含 `supplier_name`, `order_status`, `creation_date` 等） |
| `purchase_order_detail` 表 | 数据库 | 采购订单明细表（含 `item_number`, `order_quantity`, `received_quantity` 等） |
| `supplier` 表 | 数据库 | 供应商表（打印查询时需要） |
| `authenticate` 中间件 | 后端 | JWT 认证中间件 |
| `success()` 工具函数 | 后端 | 统一响应格式 `{ success: true, data, message }` |
| `request.ts` (axios 封装) | 前端 | API 调用基础封装 |
| `useColumnPreference` composable | 前端 | 列个性化（可选，可简化为固定列） |
| `ColumnSettingDrawer.vue` | 前端 | 列设置抽屉（可选，可移除） |
| `dayjs` | 前端 | 日期格式化 |

---

## 四、数据库变更

在目标系统执行以下 SQL：

```sql
-- 1. stock_in_detail 添加对账状态
IF NOT EXISTS (
  SELECT * FROM INFORMATION_SCHEMA.COLUMNS
  WHERE TABLE_NAME = 'stock_in_detail' AND COLUMN_NAME = 'reconciliation_status'
)
BEGIN
  ALTER TABLE stock_in_detail
  ADD reconciliation_status NVARCHAR(20) NOT NULL DEFAULT N'未对账';
END

-- 2. purchase_order_detail 添加对账状态
IF NOT EXISTS (
  SELECT * FROM INFORMATION_SCHEMA.COLUMNS
  WHERE TABLE_NAME = 'purchase_order_detail' AND COLUMN_NAME = 'reconciliation_status'
)
BEGIN
  ALTER TABLE purchase_order_detail
  ADD reconciliation_status NVARCHAR(20) NOT NULL DEFAULT N'未对账';
END
```

---

## 五、后端改造 — 入库单对账（stockIn）

### 5.1 Controller — 追加到 `stockIn.controller.ts`

> ✅ 已剔除 `getFactoryId`、`factory_id` 过滤、`LEFT JOIN factory`、`f.factory_name`、`f.factory_short`。

```typescript
// ==================== 采购对账（入库单明细）：分页查询 ====================
export const getPurchaseReconciliationPage = async (req: Request, res: Response, next: NextFunction) => {
  try {
    const { page = 1, limit = 20, search = '', reconciliationStatus = '' } = req.query;
    const pageNum = Number(page);
    const pageSize = Number(limit);
    const offset = (pageNum - 1) * pageSize;
    const offsetEnd = offset + pageSize;

    let whereClause = `WHERE 1=1`;
    const replacements: any = { offset, offsetEnd };

    if (reconciliationStatus) {
      const arr = String(reconciliationStatus).split(',').filter(Boolean);
      if (arr.length === 1) {
        whereClause += ` AND d.reconciliation_status = :recStatus`;
        replacements.recStatus = arr[0];
      } else if (arr.length > 1) {
        const placeholders = arr.map((_s: any, i: number) => `:recStatus${i}`).join(', ');
        whereClause += ` AND d.reconciliation_status IN (${placeholders})`;
        arr.forEach((s: string, i: number) => { replacements[`recStatus${i}`] = s; });
      }
    }
    if (search) {
      whereClause += ` AND (h.stock_in_number LIKE :search OR d.item_number LIKE :search OR d.item_name LIKE :search OR h.supplier_name LIKE :search OR h.purchase_order_number LIKE :search)`;
      replacements.search = `%${search}%`;
    }

    const [countResult]: any = await sequelize.query(
      `SELECT COUNT(*) as total
       FROM stock_in_detail d
       INNER JOIN stock_in h ON h.stock_in_number = d.stock_in_number
       ${whereClause}`, { replacements }
    );

    const [items]: any = await sequelize.query(`
      SELECT * FROM (
        SELECT d.id as detail_id, d.stock_in_number, d.line_number,
               d.purchase_order_number, d.item_number, d.item_name,
               d.specifications, d.basic_unit,
               d.stock_in_quantity, d.qualified_quantity, d.unqualified_quantity,
               d.batch_number, d.reconciliation_status,
               h.supplier_name, h.supplier_number, h.warehouse_name,
               h.stock_in_date, h.stock_in_type, h.approval_status as stock_in_status,
               h.creation_man, h.creation_date as order_creation_date,
               ROW_NUMBER() OVER (ORDER BY h.creation_date DESC, d.stock_in_number, d.line_number) AS _row_num
        FROM stock_in_detail d
        INNER JOIN stock_in h ON h.stock_in_number = d.stock_in_number
        ${whereClause}
      ) AS t WHERE t._row_num > :offset AND t._row_num <= :offsetEnd
    `, { replacements });

    res.json(success({
      items,
      total: countResult[0]?.total || 0,
      page: pageNum,
      limit: pageSize
    }));
  } catch (err) { next(err); }
};

// ==================== 采购对账（入库单明细）：批量更新对账状态 ====================
export const updatePurchaseReconciliationStatus = async (req: Request, res: Response, next: NextFunction) => {
  try {
    const { detailIds, reconciliationStatus } = req.body;

    if (!Array.isArray(detailIds) || !detailIds.length) {
      res.status(400).json({ success: false, message: '请选择要更新的记录' }); return;
    }
    if (!['已对账', '未对账'].includes(reconciliationStatus)) {
      res.status(400).json({ success: false, message: '无效的对账状态' }); return;
    }

    const replacements: any = { reconciliationStatus };
    detailIds.forEach((id: number, i: number) => { replacements[`id${i}`] = id; });
    const placeholders = detailIds.map((_: any, i: number) => `:id${i}`).join(', ');

    await sequelize.query(
      `UPDATE d SET d.reconciliation_status = :reconciliationStatus
       FROM stock_in_detail d
       INNER JOIN stock_in h ON h.stock_in_number = d.stock_in_number
       WHERE d.id IN (${placeholders})`,
      { replacements }
    );

    res.json(success(null, `已更新 ${detailIds.length} 条记录为"${reconciliationStatus}"`));
  } catch (err) { next(err); }
};

// ==================== 采购对账（入库单明细）：获取打印数据 ====================
export const getPurchaseReconciliationPrintData = async (req: Request, res: Response, next: NextFunction) => {
  try {
    const { detailIds } = req.body;

    if (!Array.isArray(detailIds) || !detailIds.length) {
      res.status(400).json({ success: false, message: '请选择要打印的记录' }); return;
    }

    const replacements: any = {};
    detailIds.forEach((id: number, i: number) => { replacements[`id${i}`] = id; });
    const placeholders = detailIds.map((_: any, i: number) => `:id${i}`).join(', ');

    const [details]: any = await sequelize.query(`
      SELECT d.id as detail_id, d.stock_in_number, d.line_number,
             d.purchase_order_number, d.item_number, d.item_name,
             d.specifications, d.basic_unit,
             d.stock_in_quantity, d.qualified_quantity, d.unqualified_quantity,
             d.batch_number, d.reconciliation_status,
             h.supplier_name, h.supplier_number, h.warehouse_name,
             h.stock_in_date, h.stock_in_type, h.approval_status,
             h.creation_man, h.creation_date as order_creation_date,
             h.remark
      FROM stock_in_detail d
      INNER JOIN stock_in h ON h.stock_in_number = d.stock_in_number
      WHERE d.id IN (${placeholders})
      ORDER BY h.stock_in_number, d.line_number
    `, { replacements });

    res.json(success({ items: details }));
  } catch (err) { next(err); }
};
```

### 5.2 Routes — 追加到 `stockIn.routes.ts`

```typescript
// import 中追加 3 个函数名：
import {
  // ... 已有 import
  getPurchaseReconciliationPage,
  updatePurchaseReconciliationStatus,
  getPurchaseReconciliationPrintData
} from './stockIn.controller';

// 在 /:id 路由之前追加（否则会被动态路由拦截）：
router.get('/reconciliation/page', authenticate, getPurchaseReconciliationPage);
router.put('/reconciliation/status', authenticate, updatePurchaseReconciliationStatus);
router.post('/reconciliation/print', authenticate, getPurchaseReconciliationPrintData);
```

---

## 六、后端改造 — 采购订单对账（purchaseOrder）

### 6.1 Controller — 追加到 `purchaseOrder.controller.ts`

```typescript
// ==================== 采购对账（采购订单明细）：分页查询 ====================
export const getPurchaseOrderReconciliationPage = async (req: Request, res: Response, next: NextFunction) => {
  try {
    const { page = 1, limit = 20, search = '', reconciliationStatus = '' } = req.query;
    const pageNum = Number(page);
    const pageSize = Number(limit);
    const offset = (pageNum - 1) * pageSize;
    const offsetEnd = offset + pageSize;

    let whereClause = `WHERE 1=1`;
    const replacements: any = { offset, offsetEnd };

    if (reconciliationStatus) {
      const arr = String(reconciliationStatus).split(',').filter(Boolean);
      if (arr.length === 1) {
        whereClause += ` AND d.reconciliation_status = :recStatus`;
        replacements.recStatus = arr[0];
      } else if (arr.length > 1) {
        const placeholders = arr.map((_s: any, i: number) => `:recStatus${i}`).join(', ');
        whereClause += ` AND d.reconciliation_status IN (${placeholders})`;
        arr.forEach((s: string, i: number) => { replacements[`recStatus${i}`] = s; });
      }
    }
    if (search) {
      whereClause += ` AND (h.purchase_order_number LIKE :search OR d.item_number LIKE :search OR d.item_name LIKE :search OR h.supplier_name LIKE :search)`;
      replacements.search = `%${search}%`;
    }

    const [countResult]: any = await sequelize.query(
      `SELECT COUNT(*) as total
       FROM purchase_order_detail d
       INNER JOIN purchase_order h ON h.purchase_order_number = d.purchase_order_number
       ${whereClause}`, { replacements }
    );

    const [items]: any = await sequelize.query(`
      SELECT * FROM (
        SELECT d.id as detail_id, d.purchase_order_number, d.line_number,
               d.item_number, d.item_name, d.specifications, d.basic_unit,
               d.order_quantity, d.received_quantity, d.receive_status,
               d.delivery_date, d.reconciliation_status,
               h.supplier_name, h.supplier_number,
               h.order_status, h.creation_man, h.creation_date as order_creation_date,
               h.approval_status,
               ROW_NUMBER() OVER (ORDER BY h.creation_date DESC, d.purchase_order_number, d.line_number) AS _row_num
        FROM purchase_order_detail d
        INNER JOIN purchase_order h ON h.purchase_order_number = d.purchase_order_number
        ${whereClause}
      ) AS t WHERE t._row_num > :offset AND t._row_num <= :offsetEnd
    `, { replacements });

    res.json(success({
      items,
      total: countResult[0]?.total || 0,
      page: pageNum,
      limit: pageSize
    }));
  } catch (err) { next(err); }
};

// ==================== 采购对账（采购订单明细）：批量更新对账状态 ====================
export const updatePurchaseOrderReconciliationStatus = async (req: Request, res: Response, next: NextFunction) => {
  try {
    const { detailIds, reconciliationStatus } = req.body;

    if (!Array.isArray(detailIds) || !detailIds.length) {
      res.status(400).json({ success: false, message: '请选择要更新的记录' }); return;
    }
    if (!['已对账', '未对账'].includes(reconciliationStatus)) {
      res.status(400).json({ success: false, message: '无效的对账状态' }); return;
    }

    const replacements: any = { reconciliationStatus };
    detailIds.forEach((id: number, i: number) => { replacements[`id${i}`] = id; });
    const placeholders = detailIds.map((_: any, i: number) => `:id${i}`).join(', ');

    await sequelize.query(
      `UPDATE d SET d.reconciliation_status = :reconciliationStatus
       FROM purchase_order_detail d
       INNER JOIN purchase_order h ON h.purchase_order_number = d.purchase_order_number
       WHERE d.id IN (${placeholders})`,
      { replacements }
    );

    res.json(success(null, `已更新 ${detailIds.length} 条记录为"${reconciliationStatus}"`));
  } catch (err) { next(err); }
};

// ==================== 采购对账（采购订单明细）：获取打印数据 ====================
export const getPurchaseOrderReconciliationPrintData = async (req: Request, res: Response, next: NextFunction) => {
  try {
    const { detailIds } = req.body;

    if (!Array.isArray(detailIds) || !detailIds.length) {
      res.status(400).json({ success: false, message: '请选择要打印的记录' }); return;
    }

    const replacements: any = {};
    detailIds.forEach((id: number, i: number) => { replacements[`id${i}`] = id; });
    const placeholders = detailIds.map((_: any, i: number) => `:id${i}`).join(', ');

    const [details]: any = await sequelize.query(`
      SELECT d.id as detail_id, d.purchase_order_number, d.line_number,
             d.item_number, d.item_name, d.specifications, d.basic_unit,
             d.order_quantity, d.received_quantity, d.receive_status,
             d.delivery_date, d.reconciliation_status,
             h.supplier_name, h.supplier_number,
             h.order_status, h.creation_man, h.creation_date as order_creation_date,
             h.remark
      FROM purchase_order_detail d
      INNER JOIN purchase_order h ON h.purchase_order_number = d.purchase_order_number
      WHERE d.id IN (${placeholders})
      ORDER BY h.purchase_order_number, d.line_number
    `, { replacements });

    res.json(success({ items: details }));
  } catch (err) { next(err); }
};
```

### 6.2 Routes — 追加到 `purchaseOrder.routes.ts`

```typescript
// import 中追加 3 个函数名：
import {
  // ... 已有 import
  getPurchaseOrderReconciliationPage,
  updatePurchaseOrderReconciliationStatus,
  getPurchaseOrderReconciliationPrintData
} from './purchaseOrder.controller';

// 在 /:id 路由之前追加：
router.get('/reconciliation/page', authenticate, getPurchaseOrderReconciliationPage);
router.put('/reconciliation/status', authenticate, updatePurchaseOrderReconciliationStatus);
router.post('/reconciliation/print', authenticate, getPurchaseOrderReconciliationPrintData);
```

### 6.3 菜单权限

在菜单权限表或迁移脚本中插入：

```typescript
{
  name: '采购对账',
  code: 'purchase-reconciliation',
  parent_code: 'purchasing',
  menu_key: 'purchase-reconciliation',
  route: '/purchase-reconciliation',
  sort: 10
}
```

---

## 七、前端改造

### 7.1 API 封装 — `api/purchasing/purchaseReconciliation.ts`

> ⚠️ 原代码中 API 参数包含 `factory_id`，需去除。完整文件如下：

```typescript
import request from '@/utils/request'

// ==================== 入库单明细行级别对账 ====================

export function getPurchaseReconciliationPage(params?: {
  page?: number; limit?: number; search?: string; reconciliationStatus?: string
}) {
  return request.get('/stock-ins/reconciliation/page', { params })
}

export function updatePurchaseReconciliationStatus(data: {
  detailIds: number[]; reconciliationStatus: string
}) {
  return request.put('/stock-ins/reconciliation/status', data)
}

export function getPurchaseReconciliationPrintData(detailIds: number[]) {
  return request.post('/stock-ins/reconciliation/print', { detailIds })
}

// ==================== 采购订单明细行级别对账 ====================

export function getPurchaseOrderReconciliationPage(params?: {
  page?: number; limit?: number; search?: string; reconciliationStatus?: string
}) {
  return request.get('/purchase-orders/reconciliation/page', { params })
}

export function updatePurchaseOrderReconciliationStatus(data: {
  detailIds: number[]; reconciliationStatus: string
}) {
  return request.put('/purchase-orders/reconciliation/status', data)
}

export function getPurchaseOrderReconciliationPrintData(detailIds: number[]) {
  return request.post('/purchase-orders/reconciliation/print', { detailIds })
}
```

### 7.2 View 页面 — `views/purchasing/PurchaseReconciliation/Index.vue`

> ⚠️ 需从原始文件中 **删除以下 5 类工厂相关代码**（变更点标注）：

#### 变更 1：删除 `getFactories` import

```typescript
// ❌ 删除这一行：
import { getFactories } from '@/api/system/factory'
```

#### 变更 2：删除工厂筛选相关的变量和函数

```typescript
// ❌ 删除以下 8 行：
const filterFactory = ref<number | undefined>(undefined)
const factoryList = ref<any[]>([])
const loadFactories = async () => {
  try {
    const res: any = await getFactories({ limit: 9999 })
    if (res.success) { factoryList.value = res.data.items || [] }
  } catch (e) { /* ignore */ }
}
```

#### 变更 3：fetchData 中删除 `factory_id` 参数

```typescript
// ❌ 删除 params 中的这一行：
factory_id: filterFactory.value,
```

#### 变更 4：删除两套列定义中的"工厂"列

```typescript
// ❌ 从 stockInDefaultColumns 中删除：
{ title: '工厂', dataIndex: 'factory_short', key: 'factory_short', width: 80, resizable: true,
  customRender: ({ record }: any) => record.factory_short || record.factory_name || '-' },

// ❌ 从 purchaseOrderDefaultColumns 中删除：
{ title: '工厂', dataIndex: 'factory_short', key: 'factory_short', width: 80, resizable: true,
  customRender: ({ record }: any) => record.factory_short || record.factory_name || '-' },
```

#### 变更 5：onMounted 中删除 `loadFactories`

```typescript
// 改为：
onMounted(async () => {
  await Promise.all([loadStockInColPref(), loadPOColPref()])
  fetchData()
})
```

#### 变更 6：模板中删除工厂筛选下拉框（L287-297）

```html
<!-- ❌ 删除整个工厂筛选 a-select 块 -->
<a-select v-model:value="filterFactory" placeholder="所属工厂" ...>
  ...
</a-select>
```

### 7.3 路由注册 — `router/purchasing.ts`

```typescript
// 顶部 import 区追加：
const PurchaseReconciliation = () => import('@/views/purchasing/PurchaseReconciliation/Index.vue');

// routes 数组中追加：
{ path: 'purchase-reconciliation', name: 'PurchaseReconciliation',
  component: PurchaseReconciliation, meta: { title: '采购对账' } },
```

---

## 八、Controller 改动对照表

为方便对照，以下汇总两个 controller 文件中需要删除的工厂相关代码：

### stockIn.controller.ts（3 个函数，共 9 处删除）

| 函数 | 删除行数 | 删除内容 |
|------|----------|----------|
| `getPurchaseReconciliationPage` | 8 行 | `getFactoryId` 过滤块（6行）+ SELECT 中 `f.factory_name, f.factory_short`（1行）+ `LEFT JOIN factory`（1行） |
| `updatePurchaseReconciliationStatus` | 4 行 | `getFactoryId` + `factoryCond`/`factoryReps`（3行）+ SQL 中 `${factoryCond}`（1处） |
| `getPurchaseReconciliationPrintData` | 4 行 | `getFactoryId` + `factoryCond`/`factoryReps`（3行）+ SQL 中 `${factoryCond}`（1处） |

### purchaseOrder.controller.ts（3 个函数，共 9 处删除）

| 函数 | 删除行数 | 删除内容 |
|------|----------|----------|
| `getPurchaseOrderReconciliationPage` | 8 行 | `getFactoryId` 过滤块（6行）+ SELECT 中 `f.factory_name, f.factory_short`（1行）+ `LEFT JOIN factory`（1行） |
| `updatePurchaseOrderReconciliationStatus` | 4 行 | `getFactoryId` + `factoryCond`/`factoryReps`（3行）+ SQL 中 `${factoryCond}`（1处） |
| `getPurchaseOrderReconciliationPrintData` | 4 行 | `getFactoryId` + `factoryCond`/`factoryReps`（3行）+ SQL 中 `${factoryCond}`（1处） |

---

## 九、可选的按需移除

| 功能 | 影响范围 | 移除方式 |
|------|----------|----------|
| **列个性化（ColumnSettingDrawer）** | 前端 View 中 `useColumnPreference` × 2 | 直接用 `stockInDefaultColumns` / `purchaseOrderDefaultColumns` |
| **列设置按钮** | 前端模板中 `<a-tooltip title="列设置">` | 删除该按钮 |
| **锁定规则中的 invoice_status** | Controller + 前端 | 如果目标系统没有开票模块，可删除 `invoice_status` 相关锁定条件 |

---

## 十、验证清单

迁移完成后逐项验证：

### 数据库
- [ ] `stock_in_detail.reconciliation_status` 列存在，默认值 `未对账`
- [ ] `purchase_order_detail.reconciliation_status` 列存在，默认值 `未对账`

### 后端 — 入库单对账
- [ ] `GET /api/stock-ins/reconciliation/page` 返回 200 + 分页数据（无 `factory_name` 字段）
- [ ] `PUT /api/stock-ins/reconciliation/status` 能成功标记/取消对账
- [ ] `POST /api/stock-ins/reconciliation/print` 返回明细数据

### 后端 — 采购订单对账
- [ ] `GET /api/purchase-orders/reconciliation/page` 返回 200 + 分页数据（无 `factory_name` 字段）
- [ ] `PUT /api/purchase-orders/reconciliation/status` 能成功标记/取消对账
- [ ] `POST /api/purchase-orders/reconciliation/print` 返回明细数据

### 前端
- [ ] 采购对账页面正常加载，两个 Tab 切换正常
- [ ] 入库单明细 Tab：列表展示、搜索、筛选、分页正常
- [ ] 采购订单明细 Tab：列表展示、搜索、筛选、分页正常
- [ ] 无"所属工厂"下拉框（已删除）
- [ ] 表格中无"工厂"列
- [ ] 勾选行 → 标记已对账/未对账 → 两个 Tab 均正常
- [ ] 勾选行 → 打印对账单 → 预览弹窗 → 浏览器打印
- [ ] 锁定行不可勾选

### 菜单
- [ ] 侧边栏出现"采购对账"菜单项，点击可进入
