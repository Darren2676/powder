# 销售对账功能迁移方案（无多工厂版）

> **适用场景**：从塑粉 MOM 系统迁移"销售对账"功能到另一个**没有多工厂隔离**的同技术栈系统。
> **技术栈要求**：Vue 3 + Ant Design Vue（前端） / Node.js + Express + Sequelize + SQL Server（后端）

---

## 一、功能概述

销售对账在发货单明细行（`shipping_order_detail`）上操作，核心三个动作：

1. **对账列表查询** — 按发货单明细行展示，支持搜索/筛选/分页
2. **批量标记对账** — 选中行 → 更新 `reconciliation_status` 为 `已对账` 或 `未对账`
3. **打印对账单** — 选中行 → 查询明细+批次 → HTML 表格 → `window.print()`

**锁定规则**：`已签收 && 已对账 && 已开票` 三条件同时满足时，行锁定不可勾选。

---

## 二、完整文件清单（共 7 处）

| # | 层级 | 文件 | 操作 |
|---|------|------|------|
| 1 | 数据库 | `shipping_order_detail` 表 | 执行 SQL 加列 |
| 2 | 后端 | `shippingOrder.controller.ts` | 追加 3 个函数（已去工厂） |
| 3 | 后端 | `shippingOrder.routes.ts` | 追加 3 条路由 |
| 4 | 后端 | 菜单权限表 | 插入菜单记录 |
| 5 | 前端 | `views/sales/SalesReconciliation/Index.vue` | 复制整个文件 |
| 6 | 前端 | `api/sales/shippingOrder.ts` | 追加 3 个函数 |
| 7 | 前端 | `router/sales.ts` | 追加路由 + import |

---

## 三、依赖预检

目标系统**必须具备**以下表和功能，否则对账页面无法正常工作：

| 依赖项 | 类型 | 说明 |
|--------|------|------|
| `shipping_order` 表 | 数据库 | 发货单主表（含 `customer_name`, `status`, `shipping_date` 等） |
| `shipping_order_detail` 表 | 数据库 | 发货单明细表（含 `item_number`, `quantity`, `sales_order_number` 等） |
| `shipping_order_batch` 表 | 数据库 | 发货批次表（打印对账单时显示批次信息） |
| `customer` 表 | 数据库 | 客户表（dataScope 按销售负责人过滤时需要） |
| `authenticate` 中间件 | 后端 | JWT 认证中间件 |
| `success()` 工具函数 | 后端 | 统一响应格式 `{ success: true, data, message }` |
| `request.ts` (axios 封装) | 前端 | API 调用基础封装 |
| `useColumnPreference` composable | 前端 | 列个性化（可选，可简化为固定列） |
| `ColumnSettingDrawer.vue` | 前端 | 列设置抽屉（可选，可移除） |
| `dayjs` | 前端 | 日期格式化 |

---

## 四、数据库变更

在目标系统执行：

```sql
IF NOT EXISTS (
  SELECT * FROM INFORMATION_SCHEMA.COLUMNS
  WHERE TABLE_NAME = 'shipping_order_detail' AND COLUMN_NAME = 'reconciliation_status'
)
BEGIN
  ALTER TABLE shipping_order_detail
  ADD reconciliation_status NVARCHAR(10) NOT NULL DEFAULT N'未对账';
END
```

---

## 五、后端改造

### 5.1 Controller — 追加 3 个对账函数

> ✅ 以下代码**已剔除多工厂隔离**（去除了 `getFactoryId`、`factory_id` 过滤、`LEFT JOIN factory`、`f.factory_name`、`f.factory_short`）。

将以下代码追加到目标系统的 `shippingOrder.controller.ts` 文件末尾：

```typescript
// ==================== 销售对账：对账明细列表（发货单明细行级别） ====================
export const getReconciliationPage = async (req: Request, res: Response, next: NextFunction) => {
  try {
    const { page = 1, limit = 20, search = '', reconciliationStatus = '' } = req.query;
    const pageNum = Number(page);
    const pageSize = Number(limit);
    const offset = (pageNum - 1) * pageSize;
    const offsetEnd = offset + pageSize;

    let whereClause = `WHERE 1=1`;
    const replacements: any = { offset, offsetEnd };

    if (reconciliationStatus) {
      const arr = String(reconciliationStatus).split(',').filter(Boolean);
      if (arr.length === 1) {
        whereClause += ` AND d.reconciliation_status = :recStatus`;
        replacements.recStatus = arr[0];
      } else if (arr.length > 1) {
        const placeholders = arr.map((_s, i) => `:recStatus${i}`).join(', ');
        whereClause += ` AND d.reconciliation_status IN (${placeholders})`;
        arr.forEach((s, i) => { replacements[`recStatus${i}`] = s; });
      }
    }
    if (search) {
      whereClause += ` AND (h.shipping_order_number LIKE :search OR d.item_number LIKE :search OR d.item_name LIKE :search OR h.customer_name LIKE :search OR d.sales_order_number LIKE :search OR h.carrier LIKE :search OR h.tracking_number LIKE :search)`;
      replacements.search = `%${search}%`;
    }

    // 数据范围过滤（可选：如果目标系统没有销售负责人概念，删除此块）
    const scope = (req as any).dataScope;
    if (scope?.head_of_sales_id) {
      whereClause += ` AND h.customer_number IN (SELECT customer_number FROM customer WHERE head_of_sales_id = :dataScopeUserId)`;
      replacements.dataScopeUserId = scope.head_of_sales_id;
    }

    const [countResult]: any = await sequelize.query(
      `SELECT COUNT(*) as total
       FROM shipping_order_detail d
       INNER JOIN shipping_order h ON h.shipping_order_number = d.shipping_order_number
       ${whereClause}`, { replacements }
    );

    const [items]: any = await sequelize.query(`
      SELECT * FROM (
        SELECT d.id as detail_id, d.shipping_order_number, d.line_number,
               d.request_number, d.sales_order_number,
               d.item_number, d.item_name, d.specifications, d.basic_unit,
               d.product_drawing_number, d.quantity as line_quantity,
               d.invoice_status, d.reconciliation_status,
               h.customer_name, h.customer_number, h.warehouse_name, h.status as order_status,
               h.shipping_date, h.carrier, h.tracking_number,
               h.creation_man, h.creation_date as order_creation_date,
               ROW_NUMBER() OVER (ORDER BY h.creation_date DESC, d.shipping_order_number, d.line_number) AS _row_num
        FROM shipping_order_detail d
        INNER JOIN shipping_order h ON h.shipping_order_number = d.shipping_order_number
        ${whereClause}
      ) AS t WHERE t._row_num > :offset AND t._row_num <= :offsetEnd
    `, { replacements });

    res.json(success({
      items,
      total: countResult[0]?.total || 0,
      page: pageNum,
      limit: pageSize
    }));
  } catch (err) { next(err); }
};

// ==================== 销售对账：批量更新对账状态 ====================
export const updateReconciliationStatus = async (req: Request, res: Response, next: NextFunction) => {
  try {
    const { detailIds, reconciliationStatus } = req.body;

    if (!Array.isArray(detailIds) || !detailIds.length) {
      res.status(400).json({ success: false, message: '请选择要更新的记录' }); return;
    }
    if (!['已对账', '未对账'].includes(reconciliationStatus)) {
      res.status(400).json({ success: false, message: '无效的对账状态' }); return;
    }

    const replacements: any = { reconciliationStatus };
    detailIds.forEach((id: number, i: number) => { replacements[`id${i}`] = id; });
    const placeholders = detailIds.map((_: any, i: number) => `:id${i}`).join(', ');

    // 检查是否存在"已签收+已对账+已开票"的已完成记录，不能修改对账状态
    const checkPlaceholders = detailIds.map((_: any, i: number) => `:pid${i}`).join(', ');
    const checkReplacements: any = {};
    detailIds.forEach((id: number, i: number) => { checkReplacements[`pid${i}`] = id; });
    const [lockedRows]: any = await sequelize.query(`
      SELECT d.id, d.shipping_order_number, d.item_number
      FROM shipping_order_detail d
      INNER JOIN shipping_order h ON h.shipping_order_number = d.shipping_order_number
      WHERE d.id IN (${checkPlaceholders})
        AND h.status = N'已签收'
        AND d.reconciliation_status = N'已对账'
        AND d.invoice_status = N'已开票'
    `, { replacements: checkReplacements });

    if (lockedRows.length > 0) {
      const lockedInfo = lockedRows.map((r: any) => `${r.shipping_order_number}/${r.item_number}`).join('、');
      res.status(400).json({
        success: false,
        message: `以下记录已签收、已对账且已开票，不能修改对账状态：${lockedInfo}`
      }); return;
    }

    await sequelize.query(
      `UPDATE d SET d.reconciliation_status = :reconciliationStatus
       FROM shipping_order_detail d
       INNER JOIN shipping_order h ON h.shipping_order_number = d.shipping_order_number
       WHERE d.id IN (${placeholders})`,
      { replacements }
    );

    res.json(success(null, `已更新 ${detailIds.length} 条记录为"${reconciliationStatus}"`));
  } catch (err) { next(err); }
};

// ==================== 销售对账：获取打印数据（选中明细行 + 批次明细） ====================
export const getReconciliationPrintData = async (req: Request, res: Response, next: NextFunction) => {
  try {
    const { detailIds } = req.body;

    if (!Array.isArray(detailIds) || !detailIds.length) {
      res.status(400).json({ success: false, message: '请选择要打印的记录' }); return;
    }

    const replacements: any = {};
    detailIds.forEach((id: number, i: number) => { replacements[`id${i}`] = id; });
    const placeholders = detailIds.map((_: any, i: number) => `:id${i}`).join(', ');

    // 查询明细行
    const [details]: any = await sequelize.query(`
      SELECT d.id as detail_id, d.shipping_order_number, d.line_number,
             d.request_number, d.sales_order_number,
             d.item_number, d.item_name, d.specifications, d.basic_unit,
             d.product_drawing_number, d.quantity as line_quantity,
             d.invoice_status, d.reconciliation_status,
             h.customer_name, h.customer_number, h.warehouse_name, h.status as order_status,
             h.shipping_date, h.carrier, h.tracking_number,
             h.creation_man, h.creation_date as order_creation_date,
             h.shipping_address, h.contact_person, h.contact_phone
      FROM shipping_order_detail d
      INNER JOIN shipping_order h ON h.shipping_order_number = d.shipping_order_number
      WHERE d.id IN (${placeholders})
      ORDER BY h.shipping_order_number, d.line_number
    `, { replacements });

    // 查询关联的批次明细
    const [batches]: any = await sequelize.query(`
      SELECT b.* FROM shipping_order_batch b
      WHERE b.detail_id IN (${placeholders})
      ORDER BY b.detail_id, b.id
    `, { replacements });

    // 将批次挂到对应明细行上
    const items = details.map((d: any) => ({
      ...d,
      batches: batches.filter((b: any) => b.detail_id === d.detail_id)
    }));

    res.json(success({ items }));
  } catch (err) { next(err); }
};
```

### 5.2 Route — 追加 3 条路由

在 `shippingOrder.routes.ts` 中追加对账路由（注意：`/reconciliation/*` 须放在 `/:shipping_order_number` 之前，否则会被动态路由拦截）：

```typescript
// 在 import 解构中加入 3 个函数名：
import {
  // ... 已有 import
  getReconciliationPage,        // ← 新增
  updateReconciliationStatus,   // ← 新增
  getReconciliationPrintData    // ← 新增
} from './shippingOrder.controller';

// 在对账路由区追加（务必在 /:shipping_order_number 之前）：
router.get('/reconciliation/page', authenticate, dataScope, getReconciliationPage);
router.put('/reconciliation/status', authenticate, updateReconciliationStatus);
router.post('/reconciliation/print', authenticate, getReconciliationPrintData);
```

### 5.3 菜单权限

在菜单权限表或迁移脚本中插入：

```typescript
{
  name: '销售对账',
  code: 'sales-reconciliation',
  parent_code: 'sales',
  menu_key: 'sales-reconciliation',
  route: '/sales-reconciliation',
  sort: 9
}
```

---

## 六、前端改造

### 6.1 View 页面

将 `client/src/views/sales/SalesReconciliation/Index.vue` 完整复制到目标系统。

> ⚠️ **需删除工厂列**（原 L50），因为后端不再返回 `factory_short`：

```typescript
// ❌ 从 defaultDataColumns 中删除这一行：
{ title: '工厂', dataIndex: 'factory_short', key: 'factory_short', width: 80, resizable: true, 
  customRender: ({ record }: any) => record.factory_short || record.factory_name || '-' },
```

### 6.2 API 封装

在 `api/sales/shippingOrder.ts`（或对应对账 API 文件）末尾追加：

```typescript
// ==================== 销售对账 ====================

export function getReconciliationPage(params?: {
  page?: number; limit?: number; search?: string; reconciliationStatus?: string
}) {
  return request.get('/shipping-orders/reconciliation/page', { params })
}

export function updateReconciliationStatus(data: {
  detailIds: number[]; reconciliationStatus: string
}) {
  return request.put('/shipping-orders/reconciliation/status', data)
}

export function getReconciliationPrintData(detailIds: number[]) {
  return request.post('/shipping-orders/reconciliation/print', { detailIds })
}
```

### 6.3 路由注册

在 `router/sales.ts` 中追加：

```typescript
// 顶部 import 区添加 lazy import：
const SalesReconciliation = () => import('@/views/sales/SalesReconciliation/Index.vue');

// routes 数组中追加：
{ path: 'sales-reconciliation', name: 'SalesReconciliation', component: SalesReconciliation,
  meta: { title: '销售对账' } },
```

---

## 七、可选的按需移除

如果目标系统没有以下功能，可进一步精简：

| 功能 | 影响范围 | 移除方式 |
|------|----------|----------|
| **dataScope 销售负责人过滤** | Controller `getReconciliationPage` 中 4 行 scope 逻辑 | 删除 if 块 |
| **列个性化（ColumnSettingDrawer）** | 前端 View 中 `useColumnPreference` 相关 | 直接使用 `defaultDataColumns`，删除 composable 调用 |
| **列设置按钮** | 前端模板中 `<a-tooltip title="列设置">` | 删除该按钮 |

---

## 八、验证清单

迁移完成后逐项验证：

- [ ] 数据库：`shipping_order_detail.reconciliation_status` 列存在，默认值 `未对账`
- [ ] 后端：`GET /api/shipping-orders/reconciliation/page` 返回 200 + 分页数据
- [ ] 后端：`PUT /api/shipping-orders/reconciliation/status` 能成功标记/取消对账
- [ ] 后端：`POST /api/shipping-orders/reconciliation/print` 返回明细 + 批次数据
- [ ] 后端：已对账+已签收+已开票的记录被锁定，返回 400 错误
- [ ] 前端：销售对账页面正常加载，列表数据展示正确
- [ ] 前端：搜索、筛选、分页功能正常
- [ ] 前端：勾选行 → 标记已对账/未对账 → 状态刷新
- [ ] 前端：勾选行 → 打印对账单 → 预览弹窗 → 浏览器打印
- [ ] 前端：锁定行（三条件满足）不可勾选
- [ ] 菜单：侧边栏出现"销售对账"菜单项，点击可进入
