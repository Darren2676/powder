# Seals MES System - 版本管理与升级手册

## 目录

- [第一部分：Git 版本控制](#第一部分git-版本控制)
- [第二部分：分支管理策略](#第二部分分支管理策略)
- [第三部分：版本号规范](#第三部分版本号规范)
- [第四部分：日常开发工作流](#第四部分日常开发工作流)
- [第五部分：依赖升级管理](#第五部分依赖升级管理)
- [第六部分：数据库版本管理](#第六部分数据库版本管理)
- [第七部分：生产环境升级流程](#第七部分生产环境升级流程)
- [第八部分：回滚方案](#第八部分回滚方案)
- [附录A：Git 常用命令速查](#附录agit-常用命令速查)
- [附录B：版本发布检查清单](#附录b版本发布检查清单)

---

## 项目概况

| 项目 | 说明 |
|------|------|
| 项目名称 | Seals MES System（橡胶密封件 MES 系统） |
| 前端技术 | Vue 3 + TypeScript + Vite + Ant Design Vue |
| 后端技术 | Node.js + Express 5 + TypeScript + Sequelize |
| 数据库 | Microsoft SQL Server 2008 R2 |
| 部署环境 | Windows Server 2012 R2 + IIS 8.5 + PM2/NSSM |

---

## 第一部分：Git 版本控制

### 1.1 初始化仓库

在项目根目录执行以下命令：

```bash
cd "D:\SealsMES\Seals MES System"
git init
```

### 1.2 配置 .gitignore

项目已有 `.gitignore` 文件，确认包含以下规则：

```gitignore
# 依赖目录
node_modules/

# 构建产物
dist/

# 环境变量（含数据库密码、JWT密钥等敏感信息）
.env

# 日志文件
*.log

# 上传文件（仅保留目录结构）
uploads/*
!uploads/.gitkeep

# 系统文件
.DS_Store
Thumbs.db
```

> **安全提醒**：`.env` 文件包含数据库密码和 JWT 密钥，**严禁**提交到版本库。应使用 `.env.example` 文件记录配置模板（不含真实密码）。

### 1.3 创建环境变量模板

在 `server/` 目录下创建 `.env.example`，供团队成员参考：

```ini
# 后端服务端口
PORT=3000

# 数据库配置
DB_HOST=127.0.0.1
DB_PORT=1433
DB_NAME=SEALSMES
DB_USER=sa
DB_PASSWORD=你的密码

# JWT 配置
JWT_SECRET=替换为随机字符串
JWT_EXPIRES_IN=24h

# 文件上传
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760
```

### 1.4 首次提交

```bash
git add .
git commit -m "初始化项目：Seals MES System v1.0.0"
```

### 1.5 连接远程仓库

根据团队使用的代码托管平台选择：

**GitHub：**
```bash
git remote add origin https://github.com/你的组织/seals-mes-system.git
git branch -M main
git push -u origin main
```

**Gitee（码云）：**
```bash
git remote add origin https://gitee.com/你的组织/seals-mes-system.git
git branch -M main
git push -u origin main
```

**自建 GitLab / 局域网 Git 服务器：**
```bash
git remote add origin http://服务器IP:端口/你的组织/seals-mes-system.git
git branch -M main
git push -u origin main
```

> **局域网部署提示**：如果工厂网络无法访问外网，可使用 [Gitea](https://gitea.io)（轻量级自托管 Git 服务）在内网服务器上搭建 Git 代码托管平台。

---

## 第二部分：分支管理策略

本项目推荐使用简化版 **Git Flow** 分支模型，适合中小型团队协作。

### 2.1 分支体系

```
main (主分支)          ─────●────────────●──────────────●──── 生产环境
                            ↑            ↑              ↑
release/x.x.x         ───[测试]───→ 合并         ───[测试]───→ 合并
                            ↑                          ↑
develop (开发分支)     ────●────●────●────●────●────●────●──── 开发集成
                          ↑    ↑         ↑
feature/xxx            ──[功能A]  [功能B]  [功能C]
                          
hotfix/xxx             ─────────────────────[紧急修复]──→ 同时合入 main 和 develop
```

### 2.2 分支说明

| 分支 | 命名规范 | 来源 | 合并目标 | 说明 |
|------|---------|------|---------|------|
| `main` | 固定名称 | — | — | **生产分支**，始终保持可部署状态，每次合并打 Tag |
| `develop` | 固定名称 | main | — | **开发集成分支**，日常开发合并到此分支 |
| `feature/*` | `feature/功能名` | develop | develop | **功能分支**，开发新功能时创建 |
| `release/*` | `release/x.x.x` | develop | main + develop | **发布分支**，准备发布时创建，用于最终测试和修复 |
| `hotfix/*` | `hotfix/问题描述` | main | main + develop | **热修复分支**，修复生产环境紧急 Bug |

### 2.3 功能分支命名示例

```
feature/login-page          # 登录页面
feature/approval-workflow   # 审批流程
feature/report-export       # 报表导出
feature/material-import     # 物料导入
hotfix/fix-login-error      # 修复登录异常
hotfix/fix-csv-encoding     # 修复CSV导出乱码
release/1.1.0               # 1.1.0 版本发布准备
release/1.2.0               # 1.2.0 版本发布准备
```

---

## 第三部分：版本号规范

### 3.1 语义化版本（Semantic Versioning）

本项目采用 **语义化版本 2.0.0** 规范，格式为：

```
主版本号.次版本号.修订号（MAJOR.MINOR.PATCH）
```

示例：`v1.2.3`

| 版本位 | 何时递增 | 举例 |
|--------|---------|------|
| **主版本号** (MAJOR) | 不兼容的重大变更：数据库结构大幅调整、API 接口不兼容、架构重构 | `v1.0.0 → v2.0.0` |
| **次版本号** (MINOR) | 向下兼容的新功能：新增模块、新增页面、新增 API | `v1.0.0 → v1.1.0` |
| **修订号** (PATCH) | 向下兼容的 Bug 修复：错误修正、样式调整、性能优化 | `v1.0.0 → v1.0.1` |

### 3.2 版本号同步位置

每次发布新版本时，需同步更新以下位置的版本号：

| 位置 | 文件路径 | 字段 |
|------|---------|------|
| 后端 | `server/package.json` | `"version"` |
| 前端 | `client/package.json` | `"version"` |
| Git Tag | — | `git tag -a v1.x.x` |

### 3.3 版本号更新操作

```bash
# 1. 更新 server/package.json 中的 version 字段
# 2. 更新 client/package.json 中的 version 字段
# 3. 提交变更
git add .
git commit -m "chore: bump version to 1.1.0"

# 4. 打标签
git tag -a v1.1.0 -m "v1.1.0 - 新增审批流程功能"

# 5. 推送标签到远程
git push origin v1.1.0
```

### 3.4 版本规划参考

| 版本 | 计划内容 | 类型 |
|------|---------|------|
| v1.0.0 | 系统初始版本：基础数据管理、工单管理、用户认证 | 初始发布 |
| v1.1.0 | 新增审批流程模块 | 功能新增 |
| v1.2.0 | 新增报表与数据分析 | 功能新增 |
| v1.2.1 | 修复 CSV 导出中文乱码问题 | Bug 修复 |
| v2.0.0 | 数据库迁移至 SQL Server 2019、API 接口重构 | 重大变更 |

---

## 第四部分：日常开发工作流

### 4.1 新功能开发流程

```bash
# 步骤1：切到 develop 分支，拉取最新代码
git checkout develop
git pull origin develop

# 步骤2：创建功能分支
git checkout -b feature/审批流程

# 步骤3：开发过程中定期提交
git add .
git commit -m "feat: 添加审批流程数据模型"

git add .
git commit -m "feat: 实现审批流程 API 接口"

git add .
git commit -m "feat: 完成审批流程前端页面"

# 步骤4：开发完成，合并回 develop
git checkout develop
git pull origin develop
git merge feature/审批流程

# 步骤5：推送到远程
git push origin develop

# 步骤6：删除功能分支（可选）
git branch -d feature/审批流程
```

### 4.2 提交信息规范

采用 **Conventional Commits** 格式，便于生成变更日志：

```
<类型>: <简短描述>

[可选正文：详细说明变更内容]
```

| 类型 | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat: 新增工单审批流程` |
| `fix` | Bug 修复 | `fix: 修复物料列表分页错误` |
| `docs` | 文档更新 | `docs: 更新部署手册` |
| `style` | 样式调整（不影响功能） | `style: 调整登录页布局` |
| `refactor` | 代码重构（不新增功能、不修复Bug） | `refactor: 重构用户认证中间件` |
| `perf` | 性能优化 | `perf: 优化物料查询接口响应速度` |
| `chore` | 构建/工具变更 | `chore: 升级 Vite 到 7.4.0` |
| `db` | 数据库变更 | `db: 新增审批记录表` |

### 4.3 版本发布流程

```bash
# 步骤1：从 develop 创建发布分支
git checkout develop
git checkout -b release/1.1.0

# 步骤2：在发布分支上进行最终测试和修复
git commit -m "fix: 修复发布前发现的表单校验问题"

# 步骤3：更新版本号
# 编辑 server/package.json 和 client/package.json 中的 version
git commit -m "chore: bump version to 1.1.0"

# 步骤4：合并到 main
git checkout main
git merge release/1.1.0
git tag -a v1.1.0 -m "v1.1.0 - 新增审批流程功能"

# 步骤5：同步合并回 develop
git checkout develop
git merge release/1.1.0

# 步骤6：推送
git push origin main
git push origin develop
git push origin v1.1.0

# 步骤7：删除发布分支
git branch -d release/1.1.0
```

### 4.4 紧急修复流程

```bash
# 步骤1：从 main 创建热修复分支
git checkout main
git checkout -b hotfix/fix-login-error

# 步骤2：修复问题
git commit -m "fix: 修复登录接口 Token 验证异常"

# 步骤3：合并到 main 并打补丁版本 Tag
git checkout main
git merge hotfix/fix-login-error
git tag -a v1.1.1 -m "v1.1.1 - 修复登录异常"

# 步骤4：同步合并回 develop
git checkout develop
git merge hotfix/fix-login-error

# 步骤5：推送并删除分支
git push origin main develop v1.1.1
git branch -d hotfix/fix-login-error
```

---

## 第五部分：依赖升级管理

### 5.1 查看可升级依赖

```bash
# 前端
cd client
npm outdated

# 后端
cd server
npm outdated
```

输出示例：

```
Package           Current   Wanted   Latest
ant-design-vue    4.2.6     4.2.8    4.3.0
vue               3.5.25    3.5.28   3.6.0
express           5.2.1     5.2.3    5.3.0
```

- **Current**：当前安装版本
- **Wanted**：`package.json` 中范围内的最新版本（安全升级）
- **Latest**：最新发布版本（可能有 breaking changes）

### 5.2 安全升级（修订号更新）

安全升级只更新修订号（PATCH），不会引入破坏性变更：

```bash
# 升级到 package.json 范围内的最新版本
npm update
```

### 5.3 功能升级（次版本更新）

升级次版本号前，**务必查阅 CHANGELOG**：

```bash
# 逐个升级，方便排查问题
npm install ant-design-vue@latest
npm install vue@latest

# 升级后立即验证
npm run build
```

### 5.4 重大升级（主版本更新）

主版本升级需在独立分支上进行，充分测试后再合并：

```bash
# 创建升级分支
git checkout -b upgrade/vue4

# 执行升级
npm install vue@next

# 根据迁移指南修改代码
# ...

# 构建测试
npm run build

# 测试通过后合并
git checkout develop
git merge upgrade/vue4
```

### 5.5 当前关键依赖版本参考

#### 前端 (client)

| 依赖 | 当前版本 | 作用 | 升级注意事项 |
|------|---------|------|-------------|
| vue | ^3.5.25 | 核心框架 | 主版本升级需全面测试 |
| ant-design-vue | ^4.2.6 | UI 组件库 | 关注组件 API 变更 |
| vite | ^7.3.1 | 构建工具 | 关注配置文件格式变化 |
| vue-router | ^4.6.4 | 路由 | 关注路由守卫 API |
| pinia | ^3.0.4 | 状态管理 | 关注 Store 定义方式 |
| axios | ^1.13.5 | HTTP 客户端 | 通常向下兼容 |
| echarts | ^6.0.0 | 图表 | 关注图表配置项变化 |
| typescript | ~5.9.3 | 类型系统 | 可能引入更严格的类型检查 |

#### 后端 (server)

| 依赖 | 当前版本 | 作用 | 升级注意事项 |
|------|---------|------|-------------|
| express | ^5.2.1 | Web 框架 | Express 5 已是大版本变更 |
| sequelize | ^6.37.7 | ORM | v7 有重大 API 变更，需谨慎 |
| tedious | ^19.2.1 | SQL Server 驱动 | 关注对 SQL Server 2008 R2 的兼容性 |
| jsonwebtoken | ^9.0.3 | JWT 认证 | 通常向下兼容 |
| bcryptjs | ^3.0.3 | 密码加密 | 通常向下兼容 |
| typescript | ^5.9.3 | 类型系统 | 保持与前端版本一致 |

### 5.6 升级安全策略

1. **升级前必须创建分支**：`git checkout -b upgrade/deps-YYYY-MM`
2. **逐步升级**：不要一次性升级所有依赖，按模块分批进行
3. **升级后验证构建**：前端 `npm run build`，后端 `npm run build`
4. **升级后验证功能**：核心流程（登录、物料管理、工单操作）均需手动验证
5. **锁定版本**：升级完成后提交 `package-lock.json`，确保团队环境一致

---

## 第六部分：数据库版本管理

### 6.1 当前数据库管理方式

项目使用 Sequelize ORM，当前通过 `sync()` 自动同步模型到数据库。此方式适合开发阶段，但**不适合生产环境**，原因：

- 无法记录变更历史
- 无法回滚特定变更
- 多人协作时可能冲突
- 数据丢失风险（`sync({ force: true })` 会删除重建表）

### 6.2 推荐方案：Sequelize 迁移（Migrations）

#### 安装 Sequelize CLI

```bash
cd server
npm install --save-dev sequelize-cli
```

#### 初始化迁移目录

```bash
npx sequelize-cli init
```

会创建以下目录：

```
server/
├── config/         # 数据库配置
├── migrations/     # 迁移脚本
├── models/         # 模型定义
└── seeders/        # 种子数据
```

#### 创建迁移脚本

```bash
# 示例：新增审批记录表
npx sequelize-cli migration:generate --name create-approval-records
```

生成文件 `migrations/20260317-create-approval-records.js`，编写内容：

```javascript
'use strict';

module.exports = {
  // 升级操作
  async up(queryInterface, Sequelize) {
    await queryInterface.createTable('approval_records', {
      id: {
        type: Sequelize.INTEGER,
        primaryKey: true,
        autoIncrement: true
      },
      ticket_id: {
        type: Sequelize.INTEGER,
        allowNull: false
      },
      approver_id: {
        type: Sequelize.INTEGER,
        allowNull: false
      },
      status: {
        type: Sequelize.STRING(20),
        allowNull: false
      },
      comment: {
        type: Sequelize.TEXT
      },
      created_at: {
        type: Sequelize.DATE,
        defaultValue: Sequelize.literal('GETDATE()')
      }
    });
  },

  // 回滚操作
  async down(queryInterface) {
    await queryInterface.dropTable('approval_records');
  }
};
```

#### 执行与回滚

```bash
# 执行所有待执行的迁移
npx sequelize-cli db:migrate

# 回滚最近一次迁移
npx sequelize-cli db:migrate:undo

# 回滚所有迁移
npx sequelize-cli db:migrate:undo:all

# 查看迁移状态
npx sequelize-cli db:migrate:status
```

### 6.3 数据库变更规范

1. **所有数据库结构变更**都必须通过迁移脚本执行，**禁止**直接在 SSMS 中手动修改表结构
2. **迁移脚本必须包含 `up` 和 `down`**，确保可以回滚
3. **迁移脚本文件名格式**：`YYYYMMDDHHMMSS-描述.js`（自动生成）
4. **迁移脚本提交到 Git**，与代码变更一同管理
5. **生产环境部署时**，先执行迁移再更新应用代码

### 6.4 数据库备份策略

在执行任何数据库升级操作前，**务必先备份**：

```sql
-- 完整备份
BACKUP DATABASE SEALSMES
TO DISK = 'D:\Backup\SEALSMES_升级前_20260317.bak'
WITH FORMAT, COMPRESSION;
GO
```

建议备份频率：

| 备份类型 | 频率 | 保留时间 |
|---------|------|---------|
| 完整备份 | 每日 | 30 天 |
| 差异备份 | 每 4 小时 | 7 天 |
| 升级前备份 | 每次升级前 | 永久保留 |

---

## 第七部分：生产环境升级流程

### 7.1 升级前准备

#### 7.1.1 制定升级计划

| 项目 | 内容 |
|------|------|
| 版本号 | 从 v1.0.0 升级至 v1.1.0 |
| 升级内容 | 新增审批流程模块，修复 3 个已知 Bug |
| 影响范围 | 新增 2 张数据表，新增 5 个 API 接口，新增 2 个前端页面 |
| 预计停机时间 | 约 15 分钟 |
| 升级窗口 | 非生产时间（如周末或下班后） |
| 回滚方案 | 恢复数据库备份 + 回退应用版本 |

#### 7.1.2 升级清单

- [ ] 通知相关人员升级时间和预计影响
- [ ] 完成数据库完整备份
- [ ] 备份当前应用目录
- [ ] 在测试环境验证新版本
- [ ] 准备回滚脚本

### 7.2 升级执行步骤

```bash
# ===== 步骤1：备份 =====

# 备份数据库（在 SSMS 中执行）
# BACKUP DATABASE SEALSMES TO DISK = 'D:\Backup\SEALSMES_v1.0.0.bak' WITH FORMAT, COMPRESSION;

# 备份当前应用
xcopy D:\SealsMES D:\Backup\SealsMES_v1.0.0 /E /I /Y


# ===== 步骤2：停止服务 =====

# 如果使用 PM2
pm2 stop seals-mes-api

# 如果使用 NSSM/Windows 服务
net stop SealsMES-API


# ===== 步骤3：更新代码 =====

# 方式A - 使用 Git（推荐）
cd /d D:\SealsMES
git fetch origin
git checkout main
git pull origin main

# 方式B - 手动复制
# 将新版本文件覆盖到 D:\SealsMES\


# ===== 步骤4：更新后端依赖 =====

cd /d D:\SealsMES\server
npm install --production


# ===== 步骤5：编译后端 =====

# 如需要重新编译（或使用提前编译好的 dist 目录）
npm run build


# ===== 步骤6：执行数据库迁移 =====

npx sequelize-cli db:migrate


# ===== 步骤7：构建前端 =====

cd /d D:\SealsMES\client
npm install
npm run build


# ===== 步骤8：启动服务 =====

# PM2
cd /d D:\SealsMES\server
pm2 start seals-mes-api

# 或 NSSM
net start SealsMES-API


# ===== 步骤9：验证 =====

# 验证后端
curl http://localhost:3000/api

# 验证前端（浏览器访问）
# http://localhost 或 http://服务器IP
```

### 7.3 升级验证清单

| 检查项 | 验证方式 | 预期结果 |
|--------|---------|---------|
| 后端服务状态 | `pm2 list` 或服务管理器 | 服务运行中 |
| 数据库连接 | 查看后端启动日志 | "数据库连接成功" |
| API 可用性 | 访问 `http://localhost:3000/api` | 返回版本号为新版本 |
| 前端页面 | 浏览器访问首页 | 正常显示登录页 |
| 用户登录 | 使用已有账号登录 | 登录成功 |
| 旧功能正常 | 检查物料管理、工单管理等 | 数据正常显示和操作 |
| 新功能可用 | 检查新增功能模块 | 功能正常运行 |
| 数据完整性 | 对比升级前后数据条数 | 数据无丢失 |

---

## 第八部分：回滚方案

### 8.1 代码回滚

```bash
# 方式A - Git 回退到上一个版本 Tag
git checkout v1.0.0

# 方式B - 恢复备份目录
xcopy D:\Backup\SealsMES_v1.0.0 D:\SealsMES /E /I /Y
```

### 8.2 数据库回滚

```bash
# 方式A - Sequelize 迁移回滚
cd /d D:\SealsMES\server
npx sequelize-cli db:migrate:undo
```

```sql
-- 方式B - 数据库完整恢复（在 SSMS 中执行）
USE master;
GO
ALTER DATABASE SEALSMES SET SINGLE_USER WITH ROLLBACK IMMEDIATE;
GO
RESTORE DATABASE SEALSMES
FROM DISK = 'D:\Backup\SEALSMES_v1.0.0.bak'
WITH REPLACE;
GO
ALTER DATABASE SEALSMES SET MULTI_USER;
GO
```

### 8.3 回滚后操作

1. 重新编译后端：`npm run build`
2. 重新构建前端：`npm run build`
3. 重启服务
4. 执行验证清单
5. 通知相关人员回滚完成

---

## 附录A：Git 常用命令速查

### 基础操作

```bash
git status                     # 查看当前状态
git add .                      # 暂存所有变更
git commit -m "描述"            # 提交
git log --oneline -10          # 查看最近10条提交记录
git diff                       # 查看未暂存的变更
git diff --staged              # 查看已暂存的变更
```

### 分支操作

```bash
git branch                     # 查看本地分支
git branch -a                  # 查看所有分支（含远程）
git checkout -b 分支名          # 创建并切换分支
git checkout 分支名             # 切换分支
git merge 分支名                # 合并分支
git branch -d 分支名            # 删除分支
```

### 远程操作

```bash
git pull origin main           # 拉取远程 main 分支
git push origin main           # 推送到远程 main 分支
git push origin v1.1.0         # 推送标签
git fetch origin               # 获取远程更新（不合并）
```

### 标签操作

```bash
git tag                        # 查看所有标签
git tag -a v1.1.0 -m "说明"    # 创建带注释的标签
git tag -d v1.1.0              # 删除本地标签
git push origin v1.1.0         # 推送标签到远程
git push origin --tags         # 推送所有标签
```

### 撤销操作

```bash
git checkout -- 文件名          # 撤销文件修改（未暂存）
git reset HEAD 文件名           # 取消暂存
git reset --soft HEAD~1        # 撤销最近一次提交（保留变更）
git stash                      # 临时储藏当前变更
git stash pop                  # 恢复储藏的变更
```

---

## 附录B：版本发布检查清单

### 发布前

- [ ] 所有功能分支已合并到 develop
- [ ] develop 分支构建通过（前端 `npm run build` + 后端 `npm run build`）
- [ ] 已创建 release 分支
- [ ] 在测试环境完成所有功能测试
- [ ] 数据库迁移脚本已编写并测试
- [ ] `package.json` 版本号已更新（前端 + 后端）
- [ ] 所有已知 Bug 已修复或记录为已知问题

### 发布时

- [ ] 已通知相关人员升级计划
- [ ] 数据库已完整备份
- [ ] 应用目录已备份
- [ ] release 分支已合并到 main
- [ ] 已打版本 Tag（如 `v1.1.0`）
- [ ] release 分支已同步合并回 develop
- [ ] 生产环境代码已更新
- [ ] 数据库迁移已执行
- [ ] 前端已重新构建
- [ ] 后端服务已重启

### 发布后

- [ ] 后端服务运行正常
- [ ] 前端页面可正常访问
- [ ] 用户可正常登录
- [ ] 核心业务功能正常（物料、工单、审批等）
- [ ] 新功能可正常使用
- [ ] 数据完整性已验证
- [ ] 已通知相关人员升级完成
- [ ] 版本 Tag 已推送到远程仓库
