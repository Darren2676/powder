# 生产任务单拆分功能说明

## 一、功能概述

生产任务单拆分功能用于将一条生产任务单按计划数量拆分为多条任务单。

**典型业务场景**：一个大批量生产任务需要分配到不同产线、班组或班次时，通过拆分功能将原始任务单的计划数量分解到多条新任务单中。

---

## 二、功能入口

- **位置**: 生产任务单列表页面底部工具栏
- **按钮**: "拆分 (N)" - 括号内显示当前选中的记录数
- **可见条件**: 仅在 "全部" 或 "未开始" 标签页下显示该按钮
- **前置条件**: 至少勾选一条任务记录后方可点击

---

## 三、操作流程

### 步骤 1：选择任务
在生产任务单列表中，通过左侧勾选框选择需要拆分的一条或多条任务记录。

### 步骤 2：打开拆分对话框
点击底部 "拆分" 按钮，系统弹出拆分对话框。选中的记录以 **原始行** 形式加载到列表中，显示为灰色 "原始" 标签。

### 步骤 3：增加拆分记录
在每行右侧操作列中点击 **"增加"** 按钮，系统基于该行复制一条新记录。新记录显示为绿色 "新增" 标签。可对同一行多次点击 "增加" 生成多条拆分记录。

### 步骤 4：设置计划数量
- **逐行修改**: 在每行的 "新计划数量" 输入框中直接输入数量
- **批量修改**: 先勾选多行，在对话框底部输入框中填写数量，点击 "批量修改" 按钮统一设置

> 提示: 原始行的新计划数量默认为原值，用户应将其修改为拆分后的剩余数量。

### 步骤 5：确定拆分
点击 **"确定拆分"** 按钮提交，系统自动处理：

| 行类型 | 处理方式 |
|--------|---------|
| 原始行 | 更新原任务单的计划数量为用户填写的新数量 |
| 新增行 | 自动生成新任务单号，创建新的生产任务单记录 |

提交完成后显示统计信息，例如："拆分完成：更新 2 条，新增 3 条"

---

## 四、核心逻辑

### 4.1 任务单号生成规则
- **格式**: `T` + `年月日(8位)` + `3位流水号`
- **示例**: T20260311001、T20260311002
- **逻辑**: 系统查询当天已有的最大任务单号，流水号自动+1
- **说明**: 每日流水号从 001 开始，支持最多 999 条/天

### 4.2 新增记录字段继承
新增的拆分记录从原始行继承以下所有字段：

| 分类 | 字段 |
|------|------|
| 计划信息 | 生产计划批号、计划完成时间、会计期间 |
| 产品信息 | 产品编号、产品名称、单位 |
| 生产信息 | 产线、班组、班次 |
| 产品规格 | 颜色、门幅、厚度、克重、剥离 |
| 基布信息 | 基布编号、基布名称 |
| 其他 | 生产备注 |

**自动设置的字段**：
- **任务单号**: 系统自动生成新编号
- **计划数量**: 使用用户填写的新计划数量
- **优先级**: 默认为 "中"
- **状态**: 默认为 "未开始"

### 4.3 拆分结果反馈
提交完成后显示统计信息："拆分完成：更新 X 条，新增 Y 条，失败 Z 条"

---

## 五、界面结构示意

```
+-------------------------------------------------------+
|  任务拆分                                    [X 关闭]   |
+-------------------------------------------------------+
| 原始行将更新计划数量，新增行将创建新任务单                   |
|                        原始 2 条  |  新增 3 条          |
+-------------------------------------------------------+
| [ ] | 类型 | 生产计划批号 | 产品编号 | 产品名称 |         |
|     |      |            |         |         |  原计划数量 | 新计划数量 | 颜色 | ... | 操作     |
+-----+------+------------+---------+---------+-----------+-----------+------+-----+---------+
| [ ] | 原始 | P2026001   | A001    | 产品A   | 1000      | [800]     | 红   | ... | [增加]  |
| [ ] | 新增 | P2026001   | A001    | 产品A   | 1000      | [200]     | 红   | ... | [增加][删除]|
| [ ] | 原始 | P2026002   | B001    | 产品B   | 500       | [300]     | 蓝   | ... | [增加]  |
| [ ] | 新增 | P2026002   | B001    | 产品B   | 500       | [100]     | 蓝   | ... | [增加][删除]|
| [ ] | 新增 | P2026002   | B001    | 产品B   | 500       | [100]     | 蓝   | ... | [增加][删除]|
+-------------------------------------------------------+
| 批量修改：[输入新计划数量]  [批量修改 (2)]                  |
+-------------------------------------------------------+
|                    [取消]   [确定拆分 (新增 3 条)]        |
+-------------------------------------------------------+
```

**规则**:
- 原始行不可删除，仅可修改计划数量和增加新行
- 新增行可删除
- "确定拆分" 按钮仅在有新增行时可点击

---

## 六、操作规则与约束

| 规则 | 说明 |
|------|------|
| 可拆分标签页 | 仅 "全部" 和 "未开始" 标签页可使用拆分功能 |
| 原始行保护 | 原始行不可删除，仅允许修改计划数量和增加新行 |
| 新增行操作 | 新增行可以删除，也可继续在其基础上增加 |
| 提交前置条件 | 必须至少有一条新增行才可点击"确定拆分" |
| 新任务单号 | 系统自动按日期+流水号规则生成，无需手动填写 |
| 新任务状态 | 拆分产生的新任务单状态默认为"未开始" |
| 新任务优先级 | 拆分产生的新任务单优先级默认为"中" |

---

## 七、操作示例

**场景**：任务单 T20260311001，产品A，计划数量 1000，需拆分给两条产线各 500。

1. 在列表中勾选 T20260311001，点击 "拆分"
2. 对话框中看到原始行：产品A，原计划数量 1000
3. 点击 "增加" 按钮，生成一条新增行
4. 修改原始行新计划数量为 **500**
5. 修改新增行新计划数量为 **500**
6. 点击 "确定拆分"
7. 结果：原任务单 T20260311001 计划数量更新为 500，系统自动创建新任务单（如 T20260311002）计划数量 500
