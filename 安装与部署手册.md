# Seals MES System - Windows Server 安装与部署手册

## 目标环境

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows Server 2016 |
| 数据库 | SQL Server 2014 SP3 |
| 运行时 | Node.js 20.x LTS |
| Web 服务器 | IIS 10.0（用于前端静态资源托管与反向代理） |

> SQL Server 2014 在 Windows Server 2016 上获得微软官方完整支持，推荐安装 SP3 累积更新以获得最佳稳定性。

---

## 第一部分：服务器基础环境准备

### 1.1 系统更新

在开始安装之前，确保 Windows Server 已安装最新的安全补丁：

1. 打开**服务器管理器** → 左侧点击**本地服务器** → 点击 **Windows 更新**旁的链接
2. 检查并安装所有重要更新
3. 重启服务器

### 1.2 关闭 IE 增强安全配置（可选）

部署期间为方便使用浏览器下载软件，可临时关闭 IE ESC：

1. 打开**服务器管理器**
2. 左侧点击**本地服务器**
3. 找到 **IE 增强的安全配置**，点击右侧的**启用**链接
4. 管理员和用户均选择**关闭**
5. 点击**确定**
6. 部署完成后建议重新开启

### 1.3 安装 .NET Framework 3.5

SQL Server 2014 安装程序依赖 .NET Framework 3.5。Windows Server 2016 默认未启用，需手动开启：

1. 打开**服务器管理器** → **管理** → **添加角色和功能**
2. 在**功能**页面勾选 **.NET Framework 3.5 功能**
3. 点击**安装**

> 如果在线安装失败，可指定 Windows Server 2016 安装光盘的 `sources\sxs` 目录作为备用源路径。在功能页面底部点击"指定备用源路径"，填入 `D:\sources\sxs`（D: 为光盘盘符）。

### 1.4 安装 .NET Framework 4.6

Windows Server 2016 自带 .NET Framework 4.6，SQL Server 2014 SP2 及以上需要此版本。确认方法：

1. 打开**命令提示符**，执行：
```cmd
reg query "HKLM\SOFTWARE\Microsoft\NET Framework Setup\NDP\v4\Full" /v Release
```
2. 返回值 >= 393295 即为 .NET 4.6 或更高版本（Windows Server 2016 默认满足）

---

## 第二部分：SQL Server 2014 安装与配置

### 2.1 安装 SQL Server 2014

如需全新安装 SQL Server 2014：

1. 挂载或解压 SQL Server 2014 安装介质
2. 运行 `setup.exe`，选择**全新 SQL Server 独立安装**
3. 通过**功能选择**页面，至少勾选：
   - **数据库引擎服务**
   - **管理工具 - 基本**（包含 SSMS）
   - **管理工具 - 完整**（推荐）
4. **实例配置**：选择**默认实例** (MSSQLSERVER)
5. **服务器配置**：
   - SQL Server 数据库引擎：**自动**启动类型
   - SQL Server Browser：**自动**启动类型
6. **数据库引擎配置**：
   - 身份验证模式选择 **混合模式（SQL Server 和 Windows 身份验证）**
   - 设置 **sa** 账户密码（请记住此密码，后续配置需要）
   - 添加当前 Windows 用户为 SQL Server 管理员
7. 完成安装向导

> **推荐**：安装完成后，下载并安装 **SQL Server 2014 SP3（KB4022619）** 累积更新，以获得最佳稳定性和安全性。

### 2.2 确认 SQL Server 服务状态

1. 打开 **SQL Server 配置管理器**（在开始菜单中搜索 `SQLServerManager12.msc`）
2. 展开 **SQL Server 服务**，确认以下服务处于**运行**状态：
   - SQL Server (MSSQLSERVER)
   - SQL Server Browser

### 2.3 启用 TCP/IP 协议

1. 打开 **SQL Server 配置管理器**
2. 展开 **SQL Server 网络配置** → **MSSQLSERVER 的协议**
3. 确认 **TCP/IP** 状态为**已启用**
4. 双击 **TCP/IP** → **IP 地址**标签页：
   - 拉到最底部 **IPAll** 部分
   - 确认 **TCP 端口** 为 `1433`
   - 清空 **TCP 动态端口**
5. 重启 SQL Server 服务使配置生效

### 2.4 配置 SQL Server 身份验证模式

如果安装时未选择混合模式，可事后修改：

1. 打开 **SQL Server Management Studio (SSMS)**
2. 右键**服务器实例** → **属性**
3. **安全性**页面 → 选择 **SQL Server 和 Windows 身份验证模式**
4. 点击**确定**，重启 SQL Server 服务

### 2.5 配置 sa 账户

1. 在 SSMS 中展开**安全性** → **登录名**
2. 双击 **sa**
3. **常规**页面：确认或重置密码
4. **状态**页面：登录选择**启用**
5. 点击**确定**

### 2.6 创建数据库

在 SSMS 中执行以下 SQL：

```sql
CREATE DATABASE XYMES;
GO
```

> **注意**：系统中的业务表（material、product、customer、supplier 等）应已存在于数据库中。如果是全新部署，需要从开发环境导出建表脚本或数据库备份进行还原。系统应用层的表（Users、Tickets 等）会在首次启动时由 Sequelize ORM 自动创建。

### 2.7 配置 Windows 防火墙

为 SQL Server 开放端口（如需从外部访问数据库）：

1. 打开**高级安全 Windows 防火墙**（运行 `wf.msc`）
2. **入站规则** → **新建规则**
3. 规则类型：**端口**
4. 协议：**TCP**，特定本地端口：`1433`
5. 操作：**允许连接**
6. 配置文件：根据网络环境选择（建议勾选**域**和**专用**）
7. 名称：`SQL Server 1433`

> 如果 SQL Server 与应用服务部署在同一台机器上且无需外部直连数据库，可跳过此步骤。

---

## 第三部分：Node.js 安装

### 3.1 下载 Node.js

Windows Server 2016 支持 **Node.js 20.x LTS**：

1. 访问 Node.js 官方下载页：https://nodejs.org/en/download/
2. 下载 **Windows Installer (.msi)** 64-bit，版本选择 **20.x LTS**（如 20.18.x）
3. 如服务器无法访问外网，可在其他电脑下载后通过 U 盘或网络共享拷贝

> **备注**：Node.js 18.x LTS 同样兼容，如已有 18.x 安装包也可使用。

### 3.2 安装 Node.js

1. 双击 `.msi` 安装包
2. 接受许可协议，一路 **Next**
3. 安装路径建议使用默认值：`C:\Program Files\nodejs\`
4. 确保勾选 **Add to PATH**
5. 点击 **Install** 完成安装

### 3.3 验证安装

打开**命令提示符（cmd）**或 **PowerShell**，执行：

```cmd
node -v
npm -v
```

应分别显示 Node.js 和 npm 的版本号（如 `v20.18.0` 和 `10.x.x`）。

### 3.4 配置 npm 镜像（可选）

如果服务器可访问外网但速度较慢，可配置淘宝镜像：

```cmd
npm config set registry https://registry.npmmirror.com
```

---

## 第四部分：部署后端服务

### 4.1 复制项目文件

将整个 `Seals MES System` 项目文件夹复制到服务器上，建议放置在：

```
D:\SealsMES\
```

最终目录结构为：

```
D:\SealsMES\
├── client\
└── server\
```

### 4.2 安装后端依赖

打开命令提示符，执行：

```cmd
cd /d D:\SealsMES\server
npm install --production
```

> `--production` 参数只安装生产依赖，不安装 devDependencies。

如果服务器无法访问外网，可以在开发机器上运行 `npm install`，然后将整个 `node_modules` 文件夹一起复制到服务器。

### 4.3 编译 TypeScript

如果仅复制了源码（无 `dist` 目录），需要在服务器上编译：

```cmd
cd /d D:\SealsMES\server

:: 全量安装依赖（包含 devDependencies 中的 typescript）
npm install

:: 编译
npm run build
```

编译成功后会生成 `server\dist\` 目录。

> **推荐做法**：在开发机器上提前执行 `npm run build`，将编译好的 `dist` 目录一同复制到服务器，服务器上仅需 `npm install --production` 即可。

### 4.4 配置环境变量

编辑 `D:\SealsMES\server\.env` 文件：

```ini
# 后端服务端口
PORT=3000

# 数据库配置（根据实际情况修改）
# 如果数据库在独立服务器上，DB_HOST 填写数据库服务器的 IP 地址
DB_HOST=127.0.0.1
DB_PORT=1433
DB_NAME=XYMES
DB_USER=sa
DB_PASSWORD=你的sa密码

# JWT 配置（生产环境务必修改为随机字符串）
JWT_SECRET=请替换为一个随机的长字符串
JWT_EXPIRES_IN=24h

# 文件上传配置
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760
```

**安全提示**：
- `JWT_SECRET` 请使用足够长的随机字符串（建议 32 位以上）
- `DB_PASSWORD` 请使用强密码
- `.env` 文件不应提交到版本控制系统

### 4.5 创建上传目录

```cmd
mkdir D:\SealsMES\server\uploads
```

### 4.6 测试启动

```cmd
cd /d D:\SealsMES\server
node dist/server.js
```

正常输出：

```
数据库连接成功
数据库同步完成
服务器运行在端口 3000
API地址: http://localhost:3000/api
```

首次启动会自动创建管理员账号（用户名 `admin`，密码 `admin123`）。

确认启动正常后按 `Ctrl+C` 停止服务，后续使用 PM2 或 Windows 服务方式管理进程。

### 4.7 开放后端端口

在 Windows 防火墙中为后端服务开放端口：

1. 打开**高级安全 Windows 防火墙**（运行 `wf.msc`）
2. **入站规则** → **新建规则**
3. 规则类型：**端口**
4. 协议：**TCP**，特定本地端口：`3000`
5. 操作：**允许连接**
6. 名称：`Seals MES Backend 3000`

---

## 第五部分：部署前端

### 5.1 构建前端

在**开发机器**上（非服务器）执行前端构建：

```cmd
cd /d D:\SealsMES\client
npm install
npm run build
```

构建成功后生成 `client\dist\` 目录，将此目录复制到服务器的 `D:\SealsMES\client\dist\`。

> **注意**：前端构建需要完整的 devDependencies，建议在开发机器上完成构建，仅将产物 `dist` 复制到服务器。

### 5.2 使用 IIS 部署前端

#### 5.2.1 安装 IIS

1. 打开**服务器管理器** → 点击**管理** → **添加角色和功能**
2. 安装类型选择**基于角色或基于功能的安装**
3. 服务器选择当前服务器，点击**下一步**
4. 在**服务器角色**中勾选 **Web 服务器 (IIS)**
5. 弹出提示框点击**添加功能**
6. **功能**页面保持默认，点击**下一步**
7. **角色服务**页面额外勾选：
   - **应用程序开发** → **ISAPI 扩展**、**ISAPI 筛选器**
   - **常见 HTTP 功能** → **静态内容**、**默认文档**
   - **安全性** → **请求筛选**
8. 点击**安装**，等待完成

#### 5.2.2 安装 URL Rewrite 模块

前端使用 Vue Router 的 HTML5 History 模式，需要 URL 重写支持：

1. 下载 **URL Rewrite Module 2.1**：
   - https://www.iis.net/downloads/microsoft/url-rewrite
   - 选择 x64 安装包
2. 安装 `.msi` 安装包

#### 5.2.3 安装 ARR（Application Request Routing）

用于将 `/api` 请求反向代理到 Node.js 后端：

1. 下载 **Application Request Routing 3.0**：
   - https://www.iis.net/downloads/microsoft/application-request-routing
2. 安装 `.msi` 安装包
3. 安装完成后，打开 **IIS 管理器**（在服务器管理器中点击**工具** → **Internet Information Services (IIS) 管理器**）
4. 点击服务器节点 → 双击 **Application Request Routing Cache**
5. 右侧点击 **Server Proxy Settings**
6. 勾选 **Enable proxy**，点击**应用**

#### 5.2.4 创建 IIS 网站

1. 打开 **IIS 管理器**
2. 展开服务器节点，右键**网站** → **添加网站**
3. 配置如下：

| 配置项 | 值 |
|--------|-----|
| 网站名称 | SealsMES |
| 物理路径 | `D:\SealsMES\client\dist` |
| 绑定类型 | http |
| IP 地址 | 全部未分配 |
| 端口 | 80 |
| 主机名 | （留空，或填写域名） |

4. 点击**确定**
5. 如果默认的 **Default Web Site** 也在使用 80 端口，需先停止它：右键 **Default Web Site** → **停止**

#### 5.2.5 配置 URL 重写规则

在 `D:\SealsMES\client\dist\` 目录下创建 `web.config` 文件：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <system.webServer>

    <!-- URL 重写规则 -->
    <rewrite>
      <rules>
        <!-- 规则1：API 反向代理到 Node.js 后端 -->
        <rule name="API Proxy" stopProcessing="true">
          <match url="^api/(.*)" />
          <action type="Rewrite" url="http://127.0.0.1:3000/api/{R:1}" />
        </rule>

        <!-- 规则2：上传文件代理 -->
        <rule name="Uploads Proxy" stopProcessing="true">
          <match url="^uploads/(.*)" />
          <action type="Rewrite" url="http://127.0.0.1:3000/uploads/{R:1}" />
        </rule>

        <!-- 规则3：Vue Router History 模式 - 所有非文件请求返回 index.html -->
        <rule name="Vue Router" stopProcessing="true">
          <match url=".*" />
          <conditions logicalGrouping="MatchAll">
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
          </conditions>
          <action type="Rewrite" url="/index.html" />
        </rule>
      </rules>
    </rewrite>

    <!-- 静态文件 MIME 类型 -->
    <staticContent>
      <remove fileExtension=".js" />
      <mimeMap fileExtension=".js" mimeType="application/javascript" />
      <remove fileExtension=".json" />
      <mimeMap fileExtension=".json" mimeType="application/json" />
      <remove fileExtension=".woff" />
      <mimeMap fileExtension=".woff" mimeType="font/woff" />
      <remove fileExtension=".woff2" />
      <mimeMap fileExtension=".woff2" mimeType="font/woff2" />
    </staticContent>

  </system.webServer>
</configuration>
```

#### 5.2.6 设置目录权限

确保 IIS 应用程序池用户对前端目录有读取权限：

1. 右键 `D:\SealsMES\client\dist` → **属性** → **安全**
2. 点击**编辑** → **添加**
3. 输入 `IIS_IUSRS`，点击**检查名称**确认后点击**确定**
4. 授予**读取和执行**权限
5. 点击**确定**

#### 5.2.7 开放 HTTP 端口

如果无法从外部访问 IIS 站点，手动添加防火墙规则：

1. 打开**高级安全 Windows 防火墙**（运行 `wf.msc`）
2. **入站规则** → **新建规则**
3. 规则类型：**端口**
4. 协议：**TCP**，特定本地端口：`80`
5. 操作：**允许连接**
6. 名称：`HTTP 80`

---

## 第六部分：使用 PM2 管理后端进程

### 6.1 安装 PM2

```cmd
npm install -g pm2
```

> Windows 上 PM2 需要配合 `pm2-windows-startup` 实现开机自启。

### 6.2 创建 PM2 配置文件

在 `D:\SealsMES\server\` 下创建 `ecosystem.config.js`：

```javascript
module.exports = {
  apps: [{
    name: 'seals-mes-api',
    script: 'dist/server.js',
    cwd: 'D:\\SealsMES\\server',
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '1G',
    env: {
      NODE_ENV: 'production'
    }
  }]
};
```

### 6.3 启动服务

```cmd
cd /d D:\SealsMES\server
pm2 start ecosystem.config.js
```

### 6.4 常用 PM2 命令

```cmd
pm2 list                    # 查看运行中的进程
pm2 logs seals-mes-api      # 查看日志
pm2 restart seals-mes-api   # 重启服务
pm2 stop seals-mes-api      # 停止服务
pm2 delete seals-mes-api    # 删除进程
pm2 monit                   # 监控面板
```

### 6.5 配置开机自启

```cmd
npm install -g pm2-windows-startup
pm2-startup install

:: 保存当前进程列表
pm2 save
```

---

## 第七部分：替代方案 - 使用 NSSM 注册为 Windows 服务

如果不使用 PM2，可以使用 NSSM（Non-Sucking Service Manager）将 Node.js 应用注册为 Windows 服务。此方案与 Windows Server 的服务管理体系集成更好，推荐在生产环境使用。

### 7.1 下载 NSSM

1. 访问 https://nssm.cc/download
2. 下载最新版本，解压到 `D:\Tools\nssm\`

### 7.2 注册服务

打开**以管理员身份运行的命令提示符**，执行：

```cmd
D:\Tools\nssm\win64\nssm.exe install SealsMES-API
```

在弹出的配置界面中填写：

| 配置项 | 值 |
|--------|-----|
| Path | `C:\Program Files\nodejs\node.exe` |
| Startup directory | `D:\SealsMES\server` |
| Arguments | `dist\server.js` |

切换到 **Details** 标签页：

| 配置项 | 值 |
|--------|-----|
| Display name | Seals MES API Service |
| Description | Seals MES System Backend API |
| Startup type | Automatic |

切换到 **I/O** 标签页（配置日志输出）：

| 配置项 | 值 |
|--------|-----|
| Output (stdout) | `D:\SealsMES\server\logs\service-out.log` |
| Error (stderr) | `D:\SealsMES\server\logs\service-err.log` |

> 请先创建 logs 目录：`mkdir D:\SealsMES\server\logs`

点击 **Install service** 完成注册。

### 7.3 管理服务

```cmd
:: 启动
net start SealsMES-API

:: 停止
net stop SealsMES-API

:: 删除服务（如需卸载）
D:\Tools\nssm\win64\nssm.exe remove SealsMES-API confirm
```

服务注册后会随系统开机自动启动。也可在**服务器管理器** → **工具** → **服务**中查看和管理。

---

## 第八部分：验证部署

### 8.1 验证后端

在服务器上打开浏览器访问：

```
http://localhost:3000/api
```

应返回：

```json
{
  "success": true,
  "message": "工单管理系统 API",
  "version": "1.0.0"
}
```

### 8.2 验证前端

在服务器上打开浏览器访问：

```
http://localhost
```

应显示系统登录页面。使用默认管理员账号登录：

- 用户名：`admin`
- 密码：`admin123`

> Windows Server 2016 自带 IE 11 和 Edge（旧版）。日常使用建议从客户端电脑使用 Chrome、Edge（Chromium 版）或 Firefox 等现代浏览器访问。

### 8.3 从客户端访问

在局域网内的其他电脑上，通过服务器 IP 访问：

```
http://服务器IP地址
```

如无法访问，请检查：
1. Windows 防火墙是否已开放 80 端口
2. IIS 网站是否已启动
3. Node.js 后端服务是否正在运行

### 8.4 验证清单

| 检查项 | 验证方式 | 预期结果 |
|--------|----------|----------|
| SQL Server 连接 | 后端启动日志 | "数据库连接成功" |
| 后端 API | 访问 `http://localhost:3000/api` | 返回 JSON |
| 前端页面 | 访问 `http://localhost` | 显示登录页 |
| 用户登录 | admin / admin123 | 进入仪表板 |
| API 代理 | 前端登录操作 | 请求正常转发到后端 |
| 数据查询 | 进入物料管理等页面 | 正常显示数据 |
| 新增记录 | 在任一管理页面点击新建 | 数据写入成功 |
| 导出功能 | 点击导出按钮 | 下载 Excel 文件 |
| 导入功能 | 点击导入按钮上传文件 | 数据导入成功 |

---

## 第九部分：运维与故障排查

### 9.1 日志位置

| 日志 | 位置 |
|------|------|
| Node.js 应用日志 | PM2: `pm2 logs` / NSSM: `D:\SealsMES\server\logs\` |
| IIS 访问日志 | `C:\inetpub\logs\LogFiles\` |
| SQL Server 日志 | SSMS → 管理 → SQL Server 日志 |
| Windows 事件日志 | 事件查看器 → Windows 日志 → 应用程序 |

### 9.2 常见问题

#### 问题1：后端启动报 "数据库连接失败"

**排查步骤：**
1. 确认 SQL Server 服务是否运行（打开**服务**列表查看 SQL Server (MSSQLSERVER)）
2. 确认 `.env` 中数据库连接信息是否正确
3. 确认 TCP/IP 协议已启用，端口为 1433
4. 用 SSMS 测试 sa 账号是否能正常登录
5. 如果数据库在独立服务器上，确认网络连通性（`telnet 数据库IP 1433`）和防火墙规则

#### 问题2：前端页面空白

**排查步骤：**
1. 检查 `D:\SealsMES\client\dist\` 下是否有 `index.html`
2. 检查 IIS 网站是否已启动
3. 检查 URL Rewrite 模块是否已安装
4. 检查 `web.config` 文件是否存在且语法正确
5. 在 IIS 管理器中点击网站 → **浏览**，查看是否有具体错误信息

#### 问题3：前端登录失败，提示网络错误

**排查步骤：**
1. 确认 Node.js 后端是否正在运行（`pm2 list` 或在服务列表查看 SealsMES-API）
2. 确认 ARR 反向代理是否已启用
3. 在服务器上直接访问 `http://localhost:3000/api/auth/login` 确认后端可达
4. 检查 `web.config` 中 API Proxy 规则的目标地址

#### 问题4：SQL Server 2014 兼容性警告

启动时可能出现 Sequelize 的版本警告：
```
DeprecationWarning: This database engine version is not supported
```

这是 Sequelize 对较早版本 SQL Server 的提示，SQL Server 2014 的核心功能完全兼容 Sequelize 的 MSSQL 方言，不影响正常使用，可安全忽略。

#### 问题5：导入文件中文乱码

确保导入文件编码为 **UTF-8 with BOM**。系统导出的文件已自动添加 BOM 标记，如使用第三方工具编辑后重新导入，请确认保存时选择 UTF-8 编码。

#### 问题6：IIS 网站启动报 80 端口冲突

Windows Server 2016 安装 IIS 后默认创建了 Default Web Site 占用 80 端口：
1. 在 IIS 管理器中停止 **Default Web Site**
2. 或者修改 SealsMES 网站绑定为其他端口（如 8080），并相应调整防火墙

#### 问题7：SQL Server 2014 安装失败

常见安装失败原因及解决方案：
1. **.NET Framework 3.5 未安装**：参见 1.3 节，使用服务器管理器添加 .NET 3.5 功能
2. **安装账户权限不足**：使用本地管理员账户运行安装程序
3. **之前安装残留**：使用微软"SQL Server 安装中心"中的"卸载"功能清理，或手动删除注册表残留项
4. **Windows Update 缺失**：确保 Windows Server 2016 已安装最新累积更新

### 9.3 备份策略

建议定期备份以下内容：

**数据库备份**（在 SSMS 中执行或使用 T-SQL）：
```sql
BACKUP DATABASE XYMES
TO DISK = 'D:\Backup\XYMES_20260318.bak'
WITH FORMAT, COMPRESSION;
GO
```

**应用备份**：
```cmd
xcopy D:\SealsMES D:\Backup\SealsMES_%date:~0,4%%date:~5,2%%date:~8,2% /E /I /Y
```

建议配合 **Windows 任务计划程序** 设置自动定期备份：
1. 打开**服务器管理器** → **工具** → **任务计划程序**
2. 创建基本任务，设置每日/每周触发
3. 操作选择执行备份脚本

**SQL Server 维护计划**（推荐）：
SQL Server 2014 支持通过 SSMS 创建维护计划，自动执行：
- 数据库完整性检查
- 索引重建/重组
- 数据库备份（完整/差异/日志）
- 清理历史备份文件

操作路径：SSMS → 管理 → 维护计划 → 右键**新建维护计划**

---

## 附录A：网络架构参考

### 方案一：单机部署（应用与数据库在同一服务器）

```
┌──────────────────────────────────────────────┐
│        Windows Server 2016                   │
│                                              │
│  ┌──────────┐  ┌──────────┐  ┌────────────┐ │
│  │ IIS 10.0 │→ │ Node.js  │→ │ SQL Server │ │
│  │ :80      │  │ :3000    │  │ 2014       │ │
│  │          │  │          │  │ :1433      │ │
│  └──────────┘  └──────────┘  └────────────┘ │
│                                              │
└──────────────────────────────────────────────┘
         ↑ 浏览器通过 http://IP 访问
```

`.env` 配置：`DB_HOST=127.0.0.1`

### 方案二：分离部署

```
┌────────────────────────────────┐    ┌──────────────────────────────┐
│   应用服务器                    │    │   数据库服务器                 │
│   Windows Server 2016          │    │   Windows Server 2016        │
│                                │    │   / 2012 R2                  │
│  ┌──────────┐  ┌────────────┐  │    │  ┌────────────────────────┐  │
│  │ IIS 10.0 │→ │  Node.js   │──┼─→──┼─→│ SQL Server 2014       │  │
│  │ :80      │  │  :3000     │  │ 网络│  │ :1433                 │  │
│  └──────────┘  └────────────┘  │    │  └────────────────────────┘  │
└────────────────────────────────┘    └──────────────────────────────┘
```

`.env` 配置：`DB_HOST=数据库服务器IP`

---

## 附录B：版本兼容性矩阵

| 组件 | 推荐版本 | 最低版本 | 备注 |
|------|----------|----------|------|
| Windows Server | 2016 | 2016 | 2019/2022 同样兼容，Windows 11 专业版亦可 |
| SQL Server | 2014 SP3 | 2008 SP4 | 2008 需配置 tdsVersion: '7_2'，详见补充文档 |
| Node.js | 20.x LTS | 18.x LTS | 20.x 在 Server 2016 上完整支持 |
| IIS | 10.0 | 10.0 | Windows Server 2016 自带 |
| URL Rewrite | 2.1 | 2.1 | 需单独下载安装 |
| ARR | 3.0 | 3.0 | 需单独下载安装 |
| SSMS | 18.x / 19.x | 2014 自带 | 建议下载最新版独立 SSMS |
| .NET Framework | 4.6+ (自带) | 3.5 + 4.6 | 3.5 需手动启用 |

---

## 附录C：部署快速检查清单

- [ ] Windows Server 2016 已完成系统更新
- [ ] .NET Framework 3.5 功能已启用
- [ ] SQL Server 2014 已安装（建议 SP3）并运行
- [ ] SQL Server TCP/IP 已启用，端口 1433
- [ ] SQL Server 混合认证模式，sa 账户已启用
- [ ] 数据库 XYMES 已创建，业务表已存在
- [ ] 防火墙开放端口：1433（如需外部访问数据库）、3000、80
- [ ] Node.js 20.x LTS 已安装
- [ ] 后端依赖已安装（`npm install`）
- [ ] 后端 TypeScript 已编译（`dist/` 目录存在）
- [ ] `.env` 文件已正确配置数据库连接信息和 JWT 密钥
- [ ] 后端服务可正常启动并连接数据库
- [ ] 前端已构建（`client\dist\` 目录存在）
- [ ] IIS 10.0 已安装，URL Rewrite 和 ARR 模块已安装
- [ ] ARR 已启用 Enable proxy
- [ ] IIS 网站已创建，指向 `client\dist`
- [ ] `web.config` 已配置（API 代理 + History 模式）
- [ ] IIS_IUSRS 对 `client\dist` 目录有读取权限
- [ ] 后端已注册为 Windows 服务（NSSM）或 PM2 管理
- [ ] 可通过客户端现代浏览器正常访问系统并登录
- [ ] 登录后各功能模块可正常查询和操作数据
