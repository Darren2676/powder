# 工艺路线主表管理手册

## 目录

- [一、功能概述](#一功能概述)
- [二、页面布局](#二页面布局)
- [三、主表操作](#三主表操作)
- [四、工序明细操作](#四工序明细操作)
- [五、流程图视图](#五流程图视图)
- [六、审批流程](#六审批流程)
- [七、导入导出](#七导入导出)
- [八、数据结构](#八数据结构)
- [九、API 接口参考](#九api-接口参考)
- [十、常见问题](#十常见问题)

---

## 一、功能概述

工艺路线主表管理是 Seals MES 系统中的核心模块，用于定义产品从原材料到成品的完整生产工序流程。本模块采用**主从表结构**（主表 + 工序明细），支持审批流程管理。

### 核心能力

| 能力 | 说明 |
|------|------|
| 主从表管理 | 主表记录工艺路线基本信息，明细表记录每道工序步骤 |
| 串型流程图 | 以可视化流程图方式展示工序链路，直观查看生产流程 |
| 审批流程 | 支持草稿 → 提交审批 → 审批通过/退回 完整审批链路 |
| 数据关联 | 自动关联产品、标准工序、工作中心、仓库等基础数据 |
| 批量导入导出 | 支持 Excel 格式的主表+明细批量导入导出 |

### 访问路径

侧边栏 → **基础数据** → **产品数据管理** → **工艺路线（主从）**

URL: `/routing-masters`

---

## 二、页面布局

页面采用上下分栏布局：

```
┌─────────────────────────────────────────────────────┐
│  工艺路线管理（主表）                     [搜索] [操作按钮]  │
│ ┌─────────────────────────────────────────────────┐ │
│ │  工艺路线列表（可点击选中行）                         │ │
│ │  · C100809 | C100809 | 防尘罩组件 | 启用 | 草稿    │ │
│ │  · R200101 | 密封圈A  | O型密封圈  | 启用 | 已审批   │ │
│ └─────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────┤
│  工序明细  当前路线: C100809       [列表/流程图] [新增工序]  │
│ ┌─────────────────────────────────────────────────┐ │
│ │  列表视图：表格展示工序详情                           │ │
│ │  流程图视图：串型可视化流程展示                        │ │
│ └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

- **上半部分**：工艺路线主表列表，点击某行选中后加载对应的工序明细
- **下半部分**：工序明细区域，支持列表和流程图两种展示模式

---

## 三、主表操作

### 3.1 搜索与筛选

| 操作 | 说明 |
|------|------|
| 搜索框 | 支持按「工艺路线编号」「工艺路线名称」「产品编号」模糊搜索 |
| 审批状态筛选 | 下拉选择：全部 / 草稿 / 待审批 / 已审批 |
| 重置 | 清空搜索条件和筛选项，恢复默认列表 |

### 3.2 新建工艺路线

点击右上角 **「+ 新建」** 按钮，在弹窗中填写：

| 字段 | 说明 | 必填 |
|------|------|------|
| 工艺路线编号 | 唯一标识，建议使用产品编号或自定义规则 | 是 |
| 工艺路线名称 | 工艺路线的名称描述 | 否 |
| 产品编号 | 下拉选择，从物品主数据（成品类型）中选取，自动填充产品名称 | 否 |
| 产品名称 | 选择产品编号后自动填充，不可手动编辑 | - |
| 生产自动入库规则 | 生产完成后自动入库的规则设置 | 否 |
| 状态 | 启用 / 禁用 | 否（默认启用） |

> **注意**：新建的工艺路线默认审批状态为「草稿」，需通过审批流程后方可在生产计划中引用。

### 3.3 编辑工艺路线

- 点击操作列的 **「编辑」** 按钮
- **仅草稿状态**可编辑，待审批和已审批状态的编辑按钮将自动禁用
- 编辑弹窗额外显示「创建日期」和「创建人」（只读）

### 3.4 删除工艺路线

- 点击操作列的 **「删除」** 按钮
- **仅草稿状态**可删除
- 删除主表时将**同时删除所有关联的工序明细**
- 操作前会弹出确认提示

### 3.5 选中行查看明细

- 点击主表中的任意一行，该行高亮显示（蓝色背景）
- 下方工序明细区域自动加载该工艺路线的所有工序步骤
- 切换选中行时，明细数据自动刷新

### 3.6 主表字段说明

| 列名 | 字段 | 说明 |
|------|------|------|
| 行号 | - | 自动生成的行序号 |
| 工艺路线编号 | process_route_number | 主键，唯一标识 |
| 工艺路线名称 | process_route_name | 名称描述 |
| 产品编号 | item_number | 关联的产品编号 |
| 产品名称 | item_name | 关联的产品名称 |
| 生产自动入库规则 | production_automatic_inventory_entry_rules | 自动入库规则 |
| 状态 | condition | 启用（绿标签）/ 禁用（红标签） |
| 审批状态 | approval_status | 草稿（灰）/ 待审批（蓝）/ 已审批（绿） |
| 创建日期 | creation_date | 格式：YYYY/MM/DD HH:mm |
| 创建人 | creation_man | 创建时的登录用户名 |
| 操作 | - | 编辑、删除、审批操作、审批历史 |

---

## 四、工序明细操作

### 4.1 新增工序

1. 在上方主表中**选中一条工艺路线**（行高亮）
2. 点击工序明细区域的 **「+ 新增工序」** 按钮
3. 在弹窗中填写工序信息

| 字段 | 说明 | 必填 |
|------|------|------|
| 工序序号 | 数字，步长为 10（如 10, 20, 30...），留空自动生成 | 否（自动） |
| 标准工序编号 | 下拉选择，从标准工序管理中选取，自动填充工序名称 | 否 |
| 标准工序名称 | 自动填充，不可手动编辑 | - |
| 后置工序编号 | 下拉选择，指定当前工序完成后的下一道工序 | 否 |
| 后置工序名称 | 自动填充 | - |
| 工作中心编号 | 下拉选择，从工作中心管理中选取 | 否 |
| 工作中心名称 | 自动填充 | - |
| 超额报工比例 | 允许超额报工的比例，如 20% | 否 |
| 配料方式 | 下拉选择：备料 / 领料 | 否 |
| 工序物料投入编号 | 该工序投入的物料编号 | 否 |
| 工序物料投入数量 | 投入数量 | 否 |
| 工序物料投入单位 | 投入单位 | 否 |
| 物料损耗率 | 物料在该工序中的预期损耗率 | 否 |
| 倒冲 | 是否倒冲领料 | 否 |
| 默认仓库 | 下拉选择，从仓库管理中选取 | 否 |

### 4.2 编辑工序

- 在列表视图中点击 **「编辑」** 按钮
- 在流程图视图中**点击对应的工序节点**即可打开编辑弹窗
- 可修改所有字段

### 4.3 删除工序

- 点击 **「删除」** 按钮，确认后删除该工序
- 删除后工序序号不会自动重排

### 4.4 工序序号规则

- 序号以 **10 为步长**递增（10, 20, 30, 40...）
- 新增工序时如不填写序号，系统自动取当前最大序号 + 10
- 可手动指定任意数字，用于在两道工序之间插入新工序（如在 10 和 20 之间插入 15）

---

## 五、流程图视图

### 5.1 切换方式

在工序明细区域右上角，通过 **「列表 / 流程图」** 切换按钮切换视图模式。

### 5.2 流程图说明

```
┌──────┐     ┌──────┐     ┌──────┐     ┌──────┐
│  10  │     │  20  │     │  30  │     │  40  │
│ 硫化 │ ──→ │ 修边 │ ──→ │ 抽检 │ ──→ │ 包装 │
│ 硫化 │     │ 修边 │     │ 抽检 │     │ 包装 │
│ 备料 │     │ 备料 │     │      │     │ 备料 │
└──────┘     └──────┘     └──────┘     └──────┘
```

| 元素 | 说明 |
|------|------|
| **顶部蓝色标签** | 工序序号 |
| **标题（粗体）** | 标准工序名称 |
| **副标题（灰色）** | 工作中心名称 |
| **底部标签** | 配料方式（备料/领料） |
| **蓝色箭头** | 工序之间的流转方向 |

### 5.3 交互操作

| 操作 | 效果 |
|------|------|
| **悬停节点** | 节点上浮、阴影加深，提示可点击 |
| **点击节点** | 打开该工序的编辑弹窗 |
| **横向滚动** | 工序数量超出显示区域时，支持横向滚动查看 |

### 5.4 适用场景

- **流程评审**：直观查看整条工艺路线的工序链路是否合理
- **生产培训**：向操作人员展示产品的完整生产流程
- **问题定位**：快速识别工序瓶颈或配置错误

---

## 六、审批流程

### 6.1 审批状态流转

```
                    ┌──────────────┐
              ┌───→ │    待审批     │ ───┐
              │     └──────────────┘    │
         [提交审批]                  [审批通过]
              │                         │
              │                         ↓
      ┌───────────────┐         ┌──────────────┐
      │     草稿      │         │    已审批     │
      └───────────────┘         └──────────────┘
              ↑                         │
              │                    [反审退回]
              │     ┌──────────────┐    │
              └───← │    待审批     │ ←──┘
                    └──────────────┘
```

### 6.2 审批操作说明

| 操作 | 按钮 | 前置条件 | 效果 |
|------|------|---------|------|
| **提交审批** | 「提交」 | 当前状态为「草稿」 | 状态变更为「待审批」 |
| **审批通过** | 「通过」 | 当前状态为「待审批」 | 状态变更为「已审批」 |
| **反审退回** | 「退回」 | 当前状态为「已审批」 | 状态变更为「草稿」 |
| **撤回提交** | 「撤回」 | 当前状态为「待审批」 | 状态变更为「草稿」 |

### 6.3 审批约束

- **草稿状态**：可编辑、可删除、可提交审批
- **待审批状态**：不可编辑、不可删除、可审批通过或撤回
- **已审批状态**：不可编辑、不可删除、可反审退回
- 所有审批操作均会记录到**审批日志**，点击 🕐 图标可查看完整审批历史

### 6.4 审批历史

点击操作列最右侧的 **历史图标** 按钮，弹窗中展示该工艺路线的所有审批操作记录：

| 字段 | 说明 |
|------|------|
| 操作类型 | 提交审批 / 审批通过 / 反审退回 / 撤回提交 |
| 操作前状态 | 操作前的审批状态 |
| 操作后状态 | 操作后的审批状态 |
| 操作人 | 执行操作的用户 |
| 操作时间 | 操作的日期时间 |

---

## 七、导入导出

### 7.1 导出

1. 点击 **「导出」** 按钮
2. 选择格式：`xlsx`（推荐）或 `xls`
3. 系统导出所有工艺路线主表及其工序明细到一个 Excel 文件
4. 文件名：`routing_masters.xlsx`

导出的 Excel 结构（每行一道工序，主表信息重复展示）：

| 工艺路线编号 | 工艺路线名称 | 产品编号 | 产品名称 | ... | 工序序号 | 标准工序编号 | 标准工序名称 | ... |
|------------|------------|---------|---------|-----|---------|------------|------------|-----|
| C100809 | C100809 | C100809 | 防尘罩组件 | | 10 | LH | 硫化 | |
| C100809 | C100809 | C100809 | 防尘罩组件 | | 20 | XB | 修边 | |
| C100809 | C100809 | C100809 | 防尘罩组件 | | 30 | CJ | 抽检 | |

### 7.2 导入

1. 点击 **「导入」** 按钮
2. 选择 `.xlsx` 或 `.xls` 格式的 Excel 文件
3. 系统自动按「工艺路线编号」分组，批量创建主表和明细记录

**导入规则：**

| 规则 | 说明 |
|------|------|
| 自动分组 | 按 `工艺路线编号` 字段分组，相同编号归为同一条工艺路线 |
| 主表去重 | 如果工艺路线编号已存在，则跳过主表创建，仅追加明细 |
| 默认状态 | 导入的工艺路线审批状态默认为「草稿」 |
| 事务保证 | 使用数据库事务，导入失败时自动回滚 |
| 无效跳过 | 跳过没有工艺路线编号的空行 |

**Excel 表头模板：**

```
工艺路线编号 | 工艺路线名称 | 产品编号 | 产品名称 | 生产自动入库规则 | 状态 | 
工序序号 | 标准工序编号 | 标准工序名称 | 后置工序编号 | 后置工序名称 | 
工作中心编号 | 工作中心名称 | 超额报工比例 | 配料方式 | 
工序物料投入编号 | 工序物料投入数量 | 工序物料投入单位 | 物料损耗率 | 倒冲 | 默认仓库
```

---

## 八、数据结构

### 8.1 主表（routing_header）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `process_route_number` | VARCHAR(50) | 工艺路线编号（主键） |
| `process_route_name` | VARCHAR(255) | 工艺路线名称 |
| `item_number` | VARCHAR(50) | 产品编号 |
| `item_name` | VARCHAR(255) | 产品名称 |
| `production_automatic_inventory_entry_rules` | VARCHAR(255) | 生产自动入库规则 |
| `condition` | VARCHAR(20) | 状态（启用/禁用） |
| `creation_date` | VARCHAR(20) | 创建日期 |
| `creation_man` | VARCHAR(50) | 创建人 |
| `approval_status` | VARCHAR(20) | 审批状态（草稿/待审批/已审批） |

### 8.2 明细表（routing_detail）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | INT IDENTITY | 自增主键 |
| `process_route_number` | VARCHAR(50) | 工艺路线编号（外键） |
| `step_number` | INT | 工序序号（步长 10） |
| `standard_process_number` | VARCHAR(50) | 标准工序编号 |
| `standard_process_name` | VARCHAR(255) | 标准工序名称 |
| `post_processing_sequence_number` | VARCHAR(50) | 后置工序编号 |
| `post_processing_sequence_name` | VARCHAR(255) | 后置工序名称 |
| `work_center_number` | VARCHAR(50) | 工作中心编号 |
| `work_center_name` | VARCHAR(255) | 工作中心名称 |
| `excess_reporting_ratio` | VARCHAR(50) | 超额报工比例 |
| `ingredient_addition_method` | VARCHAR(20) | 配料方式 |
| `process_material_input_number` | VARCHAR(50) | 工序物料投入编号 |
| `process_material_input_quantity` | VARCHAR(50) | 工序物料投入数量 |
| `process_material_input_unit` | VARCHAR(50) | 工序物料投入单位 |
| `material_wastage_rate` | VARCHAR(50) | 物料损耗率 |
| `flowing_backward` | VARCHAR(50) | 倒冲 |
| `default_repository` | VARCHAR(50) | 默认仓库 |

### 8.3 审批日志表（approval_log）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | INT IDENTITY | 自增主键 |
| `module` | VARCHAR(50) | 模块名称（routing_header） |
| `record_id` | VARCHAR(50) | 关联的工艺路线编号 |
| `action` | VARCHAR(50) | 操作类型 |
| `from_status` | VARCHAR(20) | 操作前状态 |
| `to_status` | VARCHAR(20) | 操作后状态 |
| `operator` | VARCHAR(50) | 操作人 |
| `created_at` | DATETIME | 操作时间 |

---

## 九、API 接口参考

### 9.1 主表接口

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/routing-masters` | 获取主表列表（支持分页、搜索、审批状态筛选） |
| GET | `/api/routing-masters/:id` | 获取主表详情及全部工序明细 |
| POST | `/api/routing-masters` | 创建新工艺路线 |
| PUT | `/api/routing-masters/:id` | 更新工艺路线（仅草稿状态） |
| DELETE | `/api/routing-masters/:id` | 删除工艺路线及所有明细（仅草稿状态） |
| GET | `/api/routing-masters/export` | 导出 Excel（支持 xlsx/xls） |
| POST | `/api/routing-masters/import` | 导入 Excel |

### 9.2 工序明细接口

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/routing-masters/:headerId/details` | 获取指定工艺路线的工序明细 |
| POST | `/api/routing-masters/:headerId/details` | 新增工序明细 |
| PUT | `/api/routing-masters/details/:detailId` | 编辑工序明细 |
| DELETE | `/api/routing-masters/details/:detailId` | 删除工序明细 |

### 9.3 请求参数示例

**获取主表列表：**
```
GET /api/routing-masters?page=1&limit=20&search=C100809&approval_status=草稿
```

**创建工艺路线：**
```json
POST /api/routing-masters
{
  "process_route_number": "R200101",
  "process_route_name": "密封圈A工艺路线",
  "item_number": "P001",
  "item_name": "O型密封圈",
  "production_automatic_inventory_entry_rules": "Y",
  "condition": "启用"
}
```

**新增工序：**
```json
POST /api/routing-masters/R200101/details
{
  "step_number": 10,
  "standard_process_number": "HL",
  "standard_process_name": "混炼",
  "work_center_number": "HL",
  "work_center_name": "混炼",
  "ingredient_addition_method": "备料",
  "default_repository": "03"
}
```

---

## 十、常见问题

### Q1: 编辑按钮灰色不可点击？

**原因**：工艺路线的审批状态不是「草稿」。只有草稿状态的工艺路线才允许编辑和删除。

**解决**：如需修改已审批的工艺路线，需先执行「反审退回」操作将状态恢复为草稿。

### Q2: 新增工序时提示「请先选择工艺路线」？

**原因**：未在上方主表中选中任何行。

**解决**：先点击主表中的某一行（行会高亮显示），再点击「新增工序」。

### Q3: 工序序号不连续怎么办？

**说明**：工序序号不连续是正常现象。序号仅代表执行顺序，不要求连续。删除中间的工序后，序号不会自动重排。如需重新排序，可编辑各工序的序号。

### Q4: 导入 Excel 后数据不显示？

**排查步骤**：
1. 确认 Excel 表头与模板一致（中文表头）
2. 确认「工艺路线编号」列有值（空行会被跳过）
3. 如果编号已存在，不会重复创建主表，仅追加明细
4. 检查浏览器控制台是否有报错信息

### Q5: 流程图中工序顺序不对？

**原因**：流程图按「工序序号」从小到大排列。

**解决**：编辑各工序的序号，确保顺序正确。可通过点击流程图中的节点直接打开编辑弹窗修改序号。

### Q6: 如何在两道工序之间插入新工序？

**方法**：新增工序时手动填写一个介于两道工序之间的序号。例如在序号 10 和 20 之间插入新工序，可填写序号 15。

### Q7: 产品编号下拉没有数据？

**原因**：产品编号仅显示物品主数据中类型为「成品」的记录。

**解决**：前往「物品主数据」模块，确认已录入类型为「成品」的产品数据。
