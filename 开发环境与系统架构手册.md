# Seals MES System - 开发环境与系统架构手册

## 1. 项目概述

Seals MES System（睿信橡胶密封件 MES 系统）是一个基于 B/S 架构的制造执行系统，采用前后端分离开发模式。系统包含 **WEB 端**（PC 管理后台）、**APP 端**（移动端报工）和 **后端 API 服务** 三个子项目，涵盖基础数据、销售管理、生产计划、采购管理、库存管理、质量管理、报工管理等完整的生产制造流程。

```
Seals MES System/
├── server/              # 后端 API 服务
├── client/              # WEB 前端 (PC 管理后台)
├── client-mobile/       # APP 前端 (移动端报工)
└── docs/                # 文档资料
```

---

## 2. 技术栈

### 2.1 前端 - WEB 端（client）

| 类别 | 技术 | 版本 |
|------|------|------|
| 框架 | Vue 3 (Composition API + `<script setup>`) | ^3.5.25 |
| 语言 | TypeScript | ~5.9.3 |
| 构建工具 | Vite | ^7.3.1 |
| UI 组件库 | Ant Design Vue | ^4.2.6 |
| 图标库 | @ant-design/icons-vue | ^7.0.1 |
| 路由 | Vue Router | ^4.6.4 |
| 状态管理 | Pinia | ^3.0.4 |
| HTTP 客户端 | Axios | ^1.13.5 |
| 图表 | ECharts + vue-echarts | ^6.0.0 / ^8.0.1 |
| 日期处理 | Day.js | ^1.11.19 |
| Excel 处理 | xlsx / exceljs | ^0.18.5 / ^4.4.0 |
| 二维码 | qrcode | ^1.5.4 |
| 文件下载 | file-saver | ^2.0.5 |

### 2.2 前端 - APP 移动端（client-mobile）

| 类别 | 技术 | 版本 |
|------|------|------|
| 框架 | Vue 3 (Composition API) | ^3.5.25 |
| 语言 | TypeScript | ~5.9.3 |
| 构建工具 | Vite | ^7.3.1 |
| UI 组件库 | Vant | ^4.9.14 |
| 组件自动导入 | unplugin-vue-components + @vant/auto-import-resolver | ^28.5.0 / ^1.3.0 |
| 路由 | Vue Router | ^4.6.4 |
| 状态管理 | Pinia | ^3.0.4 |
| HTTP 客户端 | Axios | ^1.13.5 |
| 日期处理 | Day.js | ^1.11.19 |
| 扫码 | html5-qrcode | ^2.3.8 |

### 2.3 后端（server）

| 类别 | 技术 | 版本 |
|------|------|------|
| 运行时 | Node.js | v25.7.0 (建议 >= 18) |
| 包管理器 | npm | v11.10.1 |
| 框架 | Express | ^5.2.1 |
| 语言 | TypeScript (ES2020, CommonJS) | ^5.9.3 |
| ORM | Sequelize (主要使用 Raw SQL) | ^6.37.7 |
| 数据库驱动 | tedious (SQL Server) | ^19.2.1 |
| 认证 | JSON Web Token (jsonwebtoken) | ^9.0.3 |
| 密码加密 | bcryptjs | ^3.0.3 |
| 参数校验 | express-validator | ^7.3.1 |
| 安全中间件 | Helmet | ^8.1.0 |
| 跨域处理 | CORS | ^2.8.6 |
| 日志 | Morgan | ^1.10.1 |
| 文件上传 | Multer | ^2.0.2 |
| Excel 处理 | xlsx | ^0.18.5 |
| 热重载 | Nodemon + ts-node | ^3.1.11 / ^10.9.2 |

### 2.4 数据库

| 类别 | 说明 |
|------|------|
| 数据库类型 | Microsoft SQL Server |
| 最低版本 | SQL Server 2008 |
| 数据库名称 | XYMES |
| 默认端口 | 1433 |
| TDS 协议版本 | 7_2 (兼容 SQL Server 2008+) |
| 连接方式 | Sequelize ORM (dialect: mssql) |
| 连接池 | max: 10, min: 0, acquire: 30s, idle: 10s |

**SQL Server 2008 兼容性说明**：
- 分页查询使用 `ROW_NUMBER() OVER()`，不使用 `OFFSET/FETCH`
- 数据库迁移使用 `IF NOT EXISTS` + `try-catch`，不使用 `MERGE`
- 所有日期函数使用 `GETDATE()`、`DATEADD()`、`DATEDIFF()`

---

## 3. 项目目录结构

### 3.1 后端 (`server/src/`)

```
server/src/
├── app.ts                    # Express 应用配置（CORS、Helmet、Morgan、静态文件托管）
├── server.ts                 # 服务入口（端口监听、数据库初始化、默认管理员创建）
├── config/
│   └── database.ts           # Sequelize 数据库连接配置
├── controllers/              # 业务控制器（72 个模块）
│   ├── auth.controller.ts           # 认证（登录/注册/修改密码）
│   ├── approval.controller.ts       # 通用审批控制器（模块注册表 + 钩子机制）
│   ├── plan.controller.ts           # 生产计划管理
│   ├── forecast.controller.ts       # 销售预测管理
│   ├── mps.controller.ts            # MPS 主计划计算
│   ├── salesOrder.controller.ts     # 销售订单管理
│   ├── finishedGoods.controller.ts  # 成品库存管理
│   ├── workReport.controller.ts     # 报工管理
│   ├── processTask.controller.ts    # 工序任务管理
│   ├── bom.controller.ts            # BOM 物料清单
│   ├── itemMaster.controller.ts     # 物料主数据
│   ├── customer.controller.ts       # 客户管理
│   ├── supplier.controller.ts       # 供应商管理
│   ├── purchaseReq.controller.ts    # 采购申请
│   ├── purchaseOrder.controller.ts  # 采购订单
│   ├── stockIn.controller.ts        # 入库管理
│   ├── materialWarehouse.controller.ts  # 原材料仓库
│   └── ...                          # 其他业务控制器
├── routes/                   # 路由定义（72 个路由文件）
│   ├── index.ts              # 路由统一注册入口
│   └── *.routes.ts           # 各模块路由
├── middleware/               # 中间件
│   ├── auth.middleware.ts    # JWT 认证中间件
│   ├── role.middleware.ts    # 角色权限校验
│   ├── upload.middleware.ts  # 文件上传配置（Multer）
│   └── error.middleware.ts   # 全局错误处理
├── models/                   # 数据模型与迁移
│   └── index.ts              # Sequelize 模型定义 + 数据库自动迁移
├── services/                 # 业务服务层
├── utils/                    # 工具函数
│   ├── response.util.ts      # 统一响应格式 success()
│   ├── excel.util.ts         # Excel 导入导出（exportToExcel / parseExcelFile）
│   ├── jwt.util.ts           # JWT 签发与验证
│   ├── password.util.ts      # 密码哈希与校验（bcryptjs）
│   ├── passwordPolicy.util.ts  # 密码强度策略
│   ├── loginLog.util.ts      # 登录日志
│   └── sms.util.ts           # 短信工具
├── types/
│   └── express.d.ts          # Express Request 类型扩展
└── validators/               # 请求参数校验
```

### 3.2 WEB 前端 (`client/src/`)

```
client/src/
├── main.ts                   # 应用入口
├── App.vue                   # 根组件
├── api/                      # API 接口定义（69 个模块）
│   ├── request.ts            # Axios 实例封装（baseURL: /api, JWT 拦截器）
│   ├── auth.ts               # 认证
│   ├── approval.ts           # 审批
│   ├── plan.ts               # 生产计划
│   ├── forecast.ts           # 销售预测
│   ├── mps.ts                # MPS 主计划
│   ├── salesOrder.ts         # 销售订单
│   ├── finishedGoods.ts      # 成品库存
│   └── ...                   # 其他模块 API
├── views/                    # 页面视图（68 个模块目录）
│   ├── Auth/                 # 登录/注册
│   ├── Dashboard/            # 仪表盘
│   ├── SalesOrder/           # 销售订单
│   ├── Forecast/             # 销售预测
│   ├── MPS/                  # MPS 主计划
│   ├── Plan/                 # 生产计划
│   ├── ProcessTask/          # 工序任务
│   ├── WorkReport/           # 报工管理
│   ├── FinishedGoods/        # 成品库存
│   ├── MaterialWarehouse/    # 原材料仓库
│   ├── Bom/                  # BOM 物料清单
│   ├── ItemMaster/           # 物料主数据
│   ├── Customer/             # 客户管理
│   ├── Supplier/             # 供应商管理
│   ├── PurchaseReq/          # 采购申请
│   ├── PurchaseOrder/        # 采购订单
│   ├── StockIn/              # 入库管理
│   ├── Equipment/            # 设备管理
│   ├── Mould/                # 模具管理
│   ├── Gantt/                # 甘特图
│   ├── Workflow/             # 工作流
│   ├── InspectionSpec/       # 质检标准
│   ├── InspectionPlan/       # 检验计划
│   └── ...                   # 其他业务页面
├── components/               # 公共组件
│   ├── Layout/
│   │   ├── AppLayout.vue     # 主布局框架（页头/页脚）
│   │   └── AppSidebar.vue    # 侧边栏导航菜单
│   ├── Common/
│   │   ├── ApprovalStatusTag.vue    # 审批状态标签组件
│   │   ├── ApprovalLogModal.vue     # 审批日志弹窗组件
│   │   └── ColumnSettingDrawer.vue  # 表格列配置抽屉组件
│   ├── Upload/               # 文件上传组件
│   ├── Comment/              # 评论组件
│   └── Ticket/               # 工单组件
├── composables/              # 组合式函数
│   ├── useColumnPreference.ts  # 表格列偏好持久化（顺序/显隐/宽度）
│   └── usePasswordPolicy.ts    # 密码策略校验
├── router/
│   └── index.ts              # 路由配置（含认证守卫）
└── utils/
    └── request.ts            # Axios 封装（JWT 自动附加、401 跳转登录）
```

### 3.3 APP 移动端 (`client-mobile/src/`)

```
client-mobile/src/
├── main.ts                   # 应用入口
├── App.vue                   # 根组件
├── api/                      # API 接口
├── views/                    # 页面
│   ├── Login.vue             # 登录
│   ├── Home.vue              # 首页
│   ├── Layout.vue            # 布局框架
│   ├── Profile.vue           # 个人信息
│   ├── TaskList.vue          # 任务列表
│   ├── WorkReport.vue        # 报工
│   ├── ScanReportEntry.vue   # 扫码报工入口
│   ├── OrderWorkReport.vue   # 订单报工
│   ├── ReportHistory.vue     # 报工历史
│   ├── MaterialPrep.vue      # 备料
│   ├── ProcessPrepEntry.vue  # 工序备料入口
│   ├── ProcessPrepAction.vue # 工序备料操作
│   └── OrderDetail.vue       # 订单详情
├── components/               # 公共组件
├── router/                   # 路由
├── store/                    # Pinia 状态管理
├── styles/                   # 样式
└── utils/                    # 工具函数
```

---

## 4. 开发环境搭建

### 4.1 前置条件

- **Node.js** >= 18.x（当前使用 v25.7.0）
- **npm** >= 9.x（当前使用 v11.10.1）
- **SQL Server** 2008 或以上版本，需开启 TCP/IP 协议，端口 1433
- **Git** 用于版本控制

### 4.2 数据库配置

后端通过 `.env` 文件配置数据库连接，文件位于 `server/.env`：

```env
PORT=3000
DB_HOST=127.0.0.1
DB_PORT=1433
DB_NAME=XYMES
DB_USER=sa
DB_PASSWORD=你的密码
JWT_SECRET=你的JWT密钥
JWT_EXPIRES_IN=24h
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760
```

### 4.3 安装依赖

```bash
# 后端
cd server && npm install

# WEB 前端
cd client && npm install

# APP 移动端
cd client-mobile && npm install
```

### 4.4 启动服务

```bash
# 后端 API 服务 (端口 3000)
cd server
npm run dev          # 开发模式 (nodemon + ts-node 热重载)
npm run build        # 编译 TypeScript → dist/
npm start            # 生产模式 (运行编译后的 JS)

# WEB 前端 (默认端口 5173，被占用时自动递增)
cd client
npm run dev          # 开发模式 (Vite 开发服务器)
npm run build        # 构建生产包 → dist/
npm run preview      # 预览生产构建

# APP 移动端 (端口 5174)
cd client-mobile
npm run dev          # 开发模式 (Vite 开发服务器)
npm run build        # 类型检查 + 构建生产包 → dist/
npm run preview      # 预览生产构建
```

### 4.5 默认管理员账号

系统首次启动时会自动创建默认管理员账号：

| 字段 | 值 |
|------|------|
| 用户名 | admin |
| 密码 | admin123 |
| 角色 | admin |

### 4.6 编译与类型检查

```bash
# 后端 TypeScript 编译检查
cd server && npx tsc --noEmit

# WEB 前端类型检查
cd client && npx vue-tsc --noEmit

# APP 移动端类型检查
cd client-mobile && npx vue-tsc --noEmit
```

---

## 5. 系统架构

### 5.1 整体架构

```
┌──────────────────────────────────────────────────────┐
│               浏览器 / 移动端 Browser                   │
│     WEB: Vue 3 + Ant Design Vue                      │
│     APP: Vue 3 + Vant                                │
└─────────────────────┬────────────────────────────────┘
                      │ HTTP / Axios
                      │ (开发环境通过 Vite Proxy 转发 /api)
                      ▼
┌──────────────────────────────────────────────────────┐
│              Express 后端服务 (端口 3000)               │
│  ┌──────────┐  ┌──────────┐  ┌────────────────────┐  │
│  │Middleware │→ │  Router  │→ │    Controller      │  │
│  │(CORS,    │  │(RESTful) │  │  (业务逻辑 + 审批    │  │
│  │ Helmet,  │  │          │  │   + 钩子回调)        │  │
│  │ JWT Auth)│  │          │  │                    │  │
│  └──────────┘  └──────────┘  └────────┬───────────┘  │
│                                       │               │
│                                       ▼               │
│                            ┌────────────────────┐     │
│                            │ Sequelize Raw SQL  │     │
│                            │ (数据访问层)         │     │
│                            └────────┬───────────┘     │
└─────────────────────────────────────┼─────────────────┘
                                      │ tedious 驱动 (TDS 7.2)
                                      ▼
                           ┌────────────────────┐
                           │  SQL Server 2008+  │
                           │  数据库: XYMES      │
                           └────────────────────┘
```

### 5.2 关键设计模式

#### 通用审批系统

`approval.controller.ts` 使用 **模块注册表** (`moduleConfig`) 实现通用审批：

```typescript
const moduleConfig = {
  'sales_order':    { tableName: 'sales_order',    primaryKey: 'sales_order_number',    displayName: '销售订单' },
  'Production_plan': { tableName: 'Production_plan', primaryKey: 'production_number',    displayName: '生产计划' },
  'sales_forecast': { tableName: 'sales_forecast',  primaryKey: 'forecast_number',      displayName: '销售预测' },
  // ... 共 12 个模块
};
```

支持操作：提交审批、审批通过、反审、撤回、批量提交、批量审批、批量撤回、批量反审。

**审批状态流转**：
```
草稿 → 待审批 → 已审批
  ↑       |         |
  |    (撤回)     (反审)
  +--------+---------+
```

**钩子机制**：审批通过/反审后触发业务回调（在事务提交之后执行）：
- 报工单审批 → 回写工序任务完工数量
- 销售订单审批 → 消耗销售预测剩余量
- 销售订单反审 → 恢复销售预测剩余量

#### 数据库迁移

`models/index.ts` 使用 `IF NOT EXISTS` + `try-catch` 模式，服务启动时自动执行迁移，确保幂等：

```sql
IF NOT EXISTS (SELECT * FROM sys.tables WHERE name = 'sales_forecast')
BEGIN
  CREATE TABLE sales_forecast (...)
END
```

#### 编号生成

采用 `前缀 + YYYYMMDD + 3位序号` 模式，通过 `MAX()` 查询自增：
- 生产计划：`M20260413001`
- 销售预测：`SF-20260413-001`

#### 分页查询

兼容 SQL Server 2008，使用 `ROW_NUMBER() OVER()` 实现分页：
```sql
SELECT * FROM (
  SELECT *, ROW_NUMBER() OVER (ORDER BY id DESC) AS _row_num
  FROM table_name WHERE ...
) AS t
WHERE t._row_num > :offset AND t._row_num <= :offsetEnd
```

### 5.3 前端架构

```
App.vue
 └── Router (vue-router, 带认证守卫)
      ├── /login             → Auth/Login.vue          (无需认证)
      ├── /register          → Auth/Register.vue       (无需认证)
      └── / (AppLayout)      → 需要认证的页面
           ├── AppSidebar     (侧边栏导航菜单)
           └── <router-view>  (内容区)
                ├── 仪表盘        Dashboard
                ├── 销售管理      SalesOrder / Forecast / MPS / ShippingOrder / ReturnOrder
                ├── 生产管理      Plan / ProcessTask / WorkReport / DispatchPrint / Gantt
                ├── 采购管理      PurchaseReq / PurchaseOrder / StockIn
                ├── 库存管理      FinishedGoods / MaterialWarehouse / BatchTrace
                ├── 质量管理      InspectionSpec / InspectionPlan / Defect
                ├── 基础数据      ItemMaster / Customer / Supplier / Employee / Bom / MfgBom
                ├── 系统设置      User / Department / Workflow / ...
                └── ...
```

**核心设计模式**：
- **API 层** (`src/api/`)：每个业务模块一个文件，封装 Axios 请求
- **视图层** (`src/views/`)：每个模块一个目录，包含 `List.vue` 等页面组件
- **组合式函数** (`src/composables/`)：可复用逻辑如列偏好持久化
- **公共组件** (`src/components/Common/`)：审批标签、审批日志、列配置等跨模块复用组件
- **状态管理**：仅认证、通知等全局共享场景使用 Pinia，局部状态用 `ref`/`reactive`

---

## 6. API 约定

### 6.1 请求格式

- 基础路径：`/api`
- 认证方式：JWT Bearer Token (`Authorization: Bearer <token>`)
- 请求体：`Content-Type: application/json`
- 文件上传：`multipart/form-data`

### 6.2 统一响应格式

```json
{
  "success": true,
  "data": { ... },
  "message": "操作成功"
}
```

### 6.3 分页参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| page | number | 1 | 当前页码 |
| limit | number | 20 | 每页条数 |
| search | string | '' | 搜索关键字 |
| approval_status | string | '' | 审批状态筛选 |

### 6.4 主要 API 路由

| 模块 | 路径 | 说明 |
|------|------|------|
| 认证 | `/api/auth` | 登录/注册/当前用户/修改密码 |
| 审批 | `/api/approval` | 通用审批操作（提交/审批/反审/撤回/批量） |
| 用户管理 | `/api/users` | 用户 CRUD (需 admin) |
| 通知 | `/api/notifications` | 消息通知 |
| 物料主数据 | `/api/item-masters` | 物料 CRUD + 导入导出 |
| 客户管理 | `/api/customers` | 客户 CRUD + 导入导出 |
| 供应商管理 | `/api/suppliers` | 供应商 CRUD + 导入导出 |
| 销售订单 | `/api/sales-orders` | 订单 CRUD + 审批 + 导入导出 |
| 销售预测 | `/api/forecasts` | 预测 CRUD + 消耗记录 |
| MPS 主计划 | `/api/mps` | MPS 计算 + 导入生产计划 |
| 生产计划 | `/api/plans` | 计划 CRUD + 从订单导入 + 导入导出 |
| BOM | `/api/boms` | BOM 管理 |
| 工序任务 | `/api/process-tasks` | 工序任务 CRUD + 派工 |
| 报工 | `/api/work-reports` | 报工 CRUD + 审批回写 |
| 采购申请 | `/api/purchase-reqs` | 采购申请 CRUD |
| 采购订单 | `/api/purchase-orders` | 采购订单 CRUD |
| 入库 | `/api/stock-ins` | 入库管理 |
| 成品库存 | `/api/finished-goods` | 库存查询 + 安全库存 |
| 原材料仓库 | `/api/material-warehouse` | 原材料库存 |
| 批次追溯 | `/api/batch-trace` | 批次追溯查询 |
| 发货 | `/api/shipping-orders` | 发货管理 |
| 退货 | `/api/return-orders` | 退货管理 |
| 设备 | `/api/equipments` | 设备 CRUD |
| 模具 | `/api/moulds` | 模具 CRUD |
| 工艺路线 | `/api/routing-masters` | 工艺路线管理 |
| 工作流 | `/api/workflows` | 工作流配置 |
| 安全设置 | `/api/security` | 密码策略等安全配置 |

---

## 7. 数据库表结构

系统使用 SQL Server 数据库 `XYMES`，所有建表和字段迁移在 `models/index.ts` 启动时自动执行（`IF NOT EXISTS` 幂等模式）。

### 7.1 系统基础表

| 表名 | 说明 | 主键 |
|------|------|------|
| users | 系统用户 | id (自增) |
| notifications | 消息通知 | id (自增) |
| departments | 部门 | id (自增) |
| approval_log | 审批日志 | id (自增) |
| user_preferences | 用户偏好设置 | id (自增) |
| login_logs | 登录日志 | id (自增) |
| security_settings | 安全配置 (key-value) | id (自增) |

### 7.2 基础数据表

| 表名 | 说明 | 主键 |
|------|------|------|
| item_master | 物料主数据 | item_number |
| customer | 客户 | customer_id |
| supplier | 供应商 | supplier_id |
| employee | 员工 | employee_number |
| warehouse | 仓库 | warehouse_number |
| workshop | 车间 | workshop_id |
| productionline | 产线 | productionline_id |
| [group] | 生产小组 | group_id |
| schedules | 班次 | schedules_id |
| equipment | 设备 | equipment_number |
| mould | 模具 | mould_id |
| material | 物料 | item_number |
| product | 产品 | product_id |
| product_ext | 产品扩展信息 (胶料编号、班产定额) | item_number |
| unit | 计量单位 | - |
| customer_material_mapping | 客户物料号映射 | id (自增) |
| defect_reason | 缺陷原因 | defect_reason_number |
| defect_class | 缺陷分类 | defect_class_number |
| defect | 缺陷 | defect_number |
| quality_characteristic | 质量特性 | char_name |

> 注意：`group` 是 SQL Server 保留字，SQL 查询中需使用方括号 `[group]` 包裹。

### 7.3 工艺路线与 BOM

| 表名 | 说明 | 主键 |
|------|------|------|
| routing_header | 工艺路线主表 | process_route_number |
| routing_detail | 工艺路线明细 | id (自增) |
| routing_detail_material | 工序物料子表 | id (自增) |
| bom_header | 设计 BOM 主表 | bom_number |
| bom_detail | 设计 BOM 明细 | id (自增) |
| mfg_bom_header | 制造 BOM 主表 | bom_number |
| mfg_bom_detail | 制造 BOM 明细 | id (自增) |

### 7.4 销售管理

| 表名 | 说明 | 主键 |
|------|------|------|
| sales_order | 销售订单主表 | sales_order_number |
| sales_order_detail | 销售订单明细 | id (自增) |
| sales_forecast | 销售预测主表 | forecast_number |
| sales_forecast_detail | 销售预测明细 | id (自增) |
| forecast_consumption | 预测消耗记录 | id (自增) |
| shipping_request | 发货申请主表 | request_number |
| shipping_request_detail | 发货申请明细 | id (自增) |
| shipping_order | 发货单主表 | - |
| shipping_order_detail | 发货单明细 | id (自增) |
| return_order | 退货单主表 | - |
| return_order_detail | 退货单明细 | id (自增) |

### 7.5 生产管理

| 表名 | 说明 | 主键 |
|------|------|------|
| Production_plan | 生产计划 | production_number |
| production_order | 生产单 | production_order_number |
| process_task | 工序任务单 | process_task_number |
| material_preparation | 备料单主表 | preparation_number |
| material_preparation_detail | 备料单明细 | id (自增) |
| material_issue | 领料单 | - |
| work_report | 报工单 | work_report_number |
| outsourcing_order | 工序委外订单 | outsourcing_order_number |

### 7.6 采购管理

| 表名 | 说明 | 主键 |
|------|------|------|
| purchase_req | 采购申请主表 | purchase_req_number |
| purchase_req_detail | 采购申请明细 | id (自增) |
| purchase_order | 采购订单主表 | purchase_order_number |
| purchase_order_detail | 采购订单明细 | id (自增) |
| stock_in | 入库单主表 | stock_in_number |
| stock_in_detail | 入库单明细 | id (自增) |

### 7.7 库存管理

| 表名 | 说明 | 主键 |
|------|------|------|
| finished_goods_inventory | 成品库存 | id (自增) |
| inventory_transaction | 成品库存流水 | id (自增) |
| finished_batch_inventory | 成品批次库存 | id (自增) |
| material_inventory | 原材料库存 | id (自增) |
| material_inventory_transaction | 原材料库存流水 | id (自增) |
| material_batch_inventory | 原材料批次库存 | id (自增) |
| batch_traceability | 批次追溯关联 | id (自增) |
| batch_number_sequence | 批次号序列 | id (自增) |
| stock_count | 盘点单主表 | id (自增) |
| stock_count_detail | 盘点单明细 | id (自增) |

### 7.8 质量管理

| 表名 | 说明 | 主键 |
|------|------|------|
| inspection_spec | 生产检验规范 | spec_name |
| inspection_spec_item | 生产检验规范明细 | id (自增) |
| inspection_plan | 生产检验方案 | plan_name |
| incoming_inspect_spec | 来料检验规范 | spec_name |
| incoming_inspect_spec_item | 来料检验规范明细 | id (自增) |
| incoming_inspect_plan | 收料检验方案 | plan_name |

### 7.9 工作流

| 表名 | 说明 | 主键 |
|------|------|------|
| workflow_definitions | 流程定义 | id (自增) |
| workflow_nodes | 流程节点 | id (自增) |
| workflow_node_handlers | 节点处理人 | id (自增) |
| workflow_edges | 节点连线 | id (自增) |
| workflow_instances | 流程实例 | id (自增) |
| workflow_tasks | 流程任务 | id (自增) |
| workflow_history | 流程审批历史 | id (自增) |

---

## 8. 开发规范

### 8.1 新增业务模块步骤

**后端**：
1. 在 `controllers/` 下创建 `xxx.controller.ts`，实现业务逻辑
2. 在 `routes/` 下创建 `xxx.routes.ts`，定义 RESTful 路由
3. 在 `routes/index.ts` 中 import 并注册路由
4. 如需审批，在 `approval.controller.ts` 的 `moduleConfig` 中注册模块
5. 如需新建表，在 `models/index.ts` 中添加 `IF NOT EXISTS` 迁移语句

**前端**：
1. 在 `api/` 下创建 `xxx.ts`，封装 API 请求函数
2. 在 `views/` 下创建 `Xxx/List.vue` 页面组件
3. 在 `router/index.ts` 中添加路由配置
4. 在 `components/Layout/AppSidebar.vue` 中添加菜单项

### 8.2 前端开发规范

- **组件风格**：统一使用 `<script setup lang="ts">` 语法
- **状态管理**：局部状态使用 `ref`/`reactive`，全局共享使用 Pinia
- **API 调用**：所有接口调用封装在 `src/api/` 目录，使用统一的 Axios 实例
- **UI 组件**：WEB 端统一使用 Ant Design Vue，APP 端统一使用 Vant
- **表格列**：使用 `useColumnPreference` 组合式函数实现列配置持久化
- **审批相关**：使用 `ApprovalStatusTag` 和 `ApprovalLogModal` 公共组件
- **日期处理**：统一使用 Day.js，格式为 `YYYY-MM-DD`

### 8.3 后端开发规范

- **控制器**：每个业务模块一个 controller 文件，导出具名函数
- **路由**：每个模块一个 routes 文件，在 `routes/index.ts` 统一注册
- **数据访问**：使用 `sequelize.query()` 原生 SQL 查询（兼容 SQL Server 2008）
- **错误处理**：Controller 内 try-catch 捕获异常，通过 `next(err)` 传递到全局错误中间件
- **事务控制**：涉及多表写操作使用 `sequelize.transaction()`
- **审批状态锁**：已提交/已审批的记录禁止编辑和删除
- **Excel 导入导出**：使用 `excel.util.ts` 中的 `exportToExcel` / `parseExcelFile`

---

## 9. 构建与部署

### 9.1 构建命令

```bash
# 后端编译
cd server && npm run build      # 输出到 server/dist/

# WEB 前端构建
cd client && npm run build      # 输出到 client/dist/

# APP 移动端构建
cd client-mobile && npm run build   # 输出到 client-mobile/dist/
```

### 9.2 生产启动

后端服务自动托管 `client/dist/` 目录的静态文件，并对非 API/uploads 请求做 SPA 路由回退。因此生产环境只需启动后端即可同时提供 API 和 WEB 页面：

```bash
cd server
npm start
# 服务运行在端口 3000
# API: http://localhost:3000/api
# WEB: http://localhost:3000/
```

### 9.3 Nginx 部署（可选）

如需使用 Nginx 反向代理：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态文件
    location / {
        root /path/to/client/dist;
        try_files $uri $uri/ /index.html;
    }

    # API 反向代理
    location /api {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 文件上传目录
    location /uploads {
        proxy_pass http://127.0.0.1:3000;
    }
}
```

### 9.4 部署注意事项

- 生产环境务必修改 `.env` 中的 `JWT_SECRET` 和数据库密码
- 确保 SQL Server 允许远程连接（如部署在不同服务器）
- 建议使用 PM2 管理后端 Node.js 进程
- APP 移动端构建产物可部署到独立静态服务器或嵌入 WebView

---

## 10. 环境变量说明

后端环境变量配置文件：`server/.env`

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| PORT | 后端服务端口 | 3000 |
| DB_HOST | 数据库主机地址 | 127.0.0.1 |
| DB_PORT | 数据库端口 | 1433 |
| DB_NAME | 数据库名称 | XYMES |
| DB_USER | 数据库用户名 | sa |
| DB_PASSWORD | 数据库密码 | (需配置) |
| JWT_SECRET | JWT 签名密钥 | (需配置) |
| JWT_EXPIRES_IN | JWT 过期时间 | 24h |
| UPLOAD_DIR | 文件上传目录 | uploads |
| MAX_FILE_SIZE | 最大上传文件大小(字节) | 10485760 (10MB) |

---

## 11. 服务端口汇总

| 服务 | 开发端口 | 说明 |
|------|----------|------|
| 后端 API | 3000 | Express + nodemon 热重载 |
| WEB 前端 | 5173 | Vite 开发服务器 (被占用时自动递增) |
| APP 移动端 | 5174 | Vite 开发服务器 |
| SQL Server | 1433 | 数据库 |

---

## 12. 安全措施

- **Helmet**：HTTP 安全头（关闭 CSP 以支持内联脚本）
- **CORS**：跨域资源共享
- **JWT**：无状态令牌认证，存储于 localStorage
- **bcryptjs**：密码哈希加密
- **角色权限**：admin / manager / user 三级角色
- **密码策略**：可配置的密码强度策略
- **审批状态锁**：已提交审批或已审批的记录禁止编辑和删除
- **文件上传限制**：Multer 配置最大文件大小

---

*文档版本: 2026-04-13 | 宁国市睿信信息技术有限责任公司*
