# Seals MES System - 开发环境与系统架构手册

## 1. 项目概述

Seals MES System（橡胶密封件MES系统）是一个基于 B/S 架构的制造执行系统，采用前后端分离开发模式，用于管理橡胶密封件生产过程中的基础数据、生产计划、工单等业务。

---

## 2. 技术栈

### 2.1 前端（client）

| 类别 | 技术 | 版本 |
|------|------|------|
| 框架 | Vue 3 (Composition API + `<script setup>`) | ^3.5.25 |
| 语言 | TypeScript | ~5.9.3 |
| 构建工具 | Vite | ^7.3.1 |
| UI 组件库 | Ant Design Vue | ^4.2.6 |
| 图标库 | @ant-design/icons-vue | ^7.0.1 |
| 路由 | Vue Router | ^4.6.4 |
| 状态管理 | Pinia | ^3.0.4 |
| HTTP 客户端 | Axios | ^1.13.5 |
| 图表 | ECharts + vue-echarts | ^6.0.0 / ^8.0.1 |
| 日期处理 | Day.js | ^1.11.19 |

### 2.2 后端（server）

| 类别 | 技术 | 版本 |
|------|------|------|
| 运行时 | Node.js | 建议 >= 18 |
| 框架 | Express | ^5.2.1 |
| 语言 | TypeScript | ^5.9.3 |
| ORM | Sequelize | ^6.37.7 |
| 数据库驱动 | tedious (SQL Server) | ^19.2.1 |
| 认证 | JSON Web Token (jsonwebtoken) | ^9.0.3 |
| 密码加密 | bcryptjs | ^3.0.3 |
| 参数校验 | express-validator | ^7.3.1 |
| 安全中间件 | Helmet | ^8.1.0 |
| 跨域处理 | CORS | ^2.8.6 |
| 日志 | Morgan | ^1.10.1 |
| 文件上传 | Multer | ^2.0.2 |
| 热重载 | Nodemon + ts-node | ^3.1.11 / ^10.9.2 |

### 2.3 数据库

| 类别 | 说明 |
|------|------|
| 数据库类型 | Microsoft SQL Server |
| 数据库名称 | XYMES |
| 默认端口 | 1433 |
| 连接方式 | Sequelize ORM（dialect: mssql） |

---

## 3. 项目目录结构

```
Seals MES System/
├── client/                          # 前端项目
│   ├── public/                      # 静态资源
│   ├── src/
│   │   ├── api/                     # API 请求封装
│   │   │   ├── auth.ts              # 认证相关
│   │   │   ├── customer.ts          # 客户管理
│   │   │   ├── dashboard.ts         # 仪表板
│   │   │   ├── employee.ts          # 员工管理
│   │   │   ├── group.ts             # 小组管理
│   │   │   ├── material.ts          # 物料管理
│   │   │   ├── plan.ts              # 计划管理
│   │   │   ├── product.ts           # 产品管理
│   │   │   ├── productionline.ts    # 产线管理
│   │   │   ├── schedule.ts          # 班次管理
│   │   │   ├── supplier.ts          # 供应商管理
│   │   │   ├── ticket.ts            # 工单管理
│   │   │   ├── user.ts              # 用户管理
│   │   │   └── workshop.ts          # 车间管理
│   │   ├── components/              # 公共组件
│   │   │   ├── Layout/              # 布局组件
│   │   │   │   ├── AppLayout.vue    # 主布局框架
│   │   │   │   ├── AppHeader.vue    # 顶栏
│   │   │   │   └── AppSidebar.vue   # 侧边栏菜单
│   │   │   ├── Comment/             # 评论组件
│   │   │   ├── Common/              # 通用组件
│   │   │   ├── Ticket/              # 工单组件
│   │   │   └── Upload/              # 上传组件
│   │   ├── router/                  # 路由配置
│   │   │   └── index.ts
│   │   ├── store/                   # Pinia 状态管理
│   │   │   ├── auth.ts              # 认证状态
│   │   │   └── notification.ts      # 通知状态
│   │   ├── types/                   # TypeScript 类型定义
│   │   ├── utils/                   # 工具函数
│   │   │   └── request.ts           # Axios 实例封装
│   │   ├── views/                   # 页面视图
│   │   │   ├── Auth/                # 登录/注册
│   │   │   ├── Customer/            # 客户管理
│   │   │   ├── Dashboard/           # 仪表板
│   │   │   ├── Employee/            # 员工管理
│   │   │   ├── Group/               # 小组管理
│   │   │   ├── Material/            # 物料管理
│   │   │   ├── Plan/                # 计划管理
│   │   │   ├── Product/             # 产品管理
│   │   │   ├── Productionline/      # 产线管理
│   │   │   ├── Schedule/            # 班次管理
│   │   │   ├── Supplier/            # 供应商管理
│   │   │   ├── Ticket/              # 工单管理
│   │   │   ├── User/                # 用户管理
│   │   │   └── Workshop/            # 车间管理
│   │   ├── App.vue                  # 根组件
│   │   └── main.ts                  # 入口文件
│   ├── package.json
│   ├── vite.config.ts               # Vite 配置
│   └── tsconfig.json
│
├── server/                          # 后端项目
│   ├── src/
│   │   ├── config/
│   │   │   └── database.ts          # 数据库连接配置
│   │   ├── controllers/             # 控制器层（业务逻辑）
│   │   │   ├── auth.controller.ts
│   │   │   ├── customer.controller.ts
│   │   │   ├── dashboard.controller.ts
│   │   │   ├── employee.controller.ts
│   │   │   ├── group.controller.ts
│   │   │   ├── material.controller.ts
│   │   │   ├── plan.controller.ts
│   │   │   ├── product.controller.ts
│   │   │   ├── productionline.controller.ts
│   │   │   ├── schedule.controller.ts
│   │   │   ├── supplier.controller.ts
│   │   │   ├── ticket.controller.ts
│   │   │   ├── user.controller.ts
│   │   │   └── workshop.controller.ts
│   │   ├── middleware/              # 中间件
│   │   │   ├── auth.middleware.ts   # JWT 认证中间件
│   │   │   ├── error.middleware.ts  # 全局错误处理
│   │   │   ├── role.middleware.ts   # 角色权限校验
│   │   │   └── upload.middleware.ts # 文件上传中间件
│   │   ├── models/                  # Sequelize 数据模型
│   │   │   ├── index.ts             # 模型统一导出与数据库同步
│   │   │   ├── User.ts
│   │   │   ├── Ticket.ts
│   │   │   ├── Comment.ts
│   │   │   ├── Attachment.ts
│   │   │   └── Notification.ts
│   │   ├── routes/                  # 路由定义
│   │   │   ├── index.ts             # 路由统一注册
│   │   │   ├── auth.routes.ts
│   │   │   ├── customer.routes.ts
│   │   │   ├── employee.routes.ts
│   │   │   ├── group.routes.ts
│   │   │   ├── material.routes.ts
│   │   │   ├── plan.routes.ts
│   │   │   ├── product.routes.ts
│   │   │   ├── productionline.routes.ts
│   │   │   ├── schedule.routes.ts
│   │   │   ├── supplier.routes.ts
│   │   │   ├── ticket.routes.ts
│   │   │   ├── user.routes.ts
│   │   │   └── workshop.routes.ts
│   │   ├── types/                   # TypeScript 类型扩展
│   │   │   └── express.d.ts         # Express Request 类型扩展
│   │   ├── utils/                   # 工具函数
│   │   │   ├── jwt.util.ts          # JWT 签发与验证
│   │   │   ├── password.util.ts     # 密码哈希与校验
│   │   │   └── response.util.ts     # 统一响应格式
│   │   ├── app.ts                   # Express 应用配置
│   │   └── server.ts                # 服务器入口（启动文件）
│   ├── .env                         # 环境变量配置
│   ├── package.json
│   └── tsconfig.json
│
└── 开发环境与系统架构手册.md          # 本文档
```

---

## 4. 开发环境搭建

### 4.1 前置条件

- **Node.js**: >= 18.x（建议使用 LTS 版本）
- **npm**: >= 9.x
- **SQL Server**: 已安装并运行，数据库 `XYMES` 已创建
- **Git**: 用于版本控制

### 4.2 后端启动

```bash
# 1. 进入后端目录
cd server

# 2. 安装依赖
npm install

# 3. 配置环境变量（编辑 .env 文件）
#    根据实际数据库信息修改以下内容：
#    PORT=3000
#    DB_HOST=127.0.0.1
#    DB_PORT=1433
#    DB_NAME=XYMES
#    DB_USER=sa
#    DB_PASSWORD=your_password
#    JWT_SECRET=your_jwt_secret
#    JWT_EXPIRES_IN=24h
#    UPLOAD_DIR=uploads
#    MAX_FILE_SIZE=10485760

# 4. 开发模式启动（支持热重载）
npm run dev

# 5. 生产构建
npm run build

# 6. 生产模式启动
npm run start
```

启动成功后，后端服务运行在 `http://localhost:3000`，API 基础路径为 `http://localhost:3000/api`。

首次启动时，系统会自动创建默认管理员账号：
- 用户名：`admin`
- 密码：`admin123`

### 4.3 前端启动

```bash
# 1. 进入前端目录
cd client

# 2. 安装依赖
npm install

# 3. 开发模式启动
npm run dev

# 4. 生产构建
npm run build

# 5. 预览生产构建产物
npm run preview
```

启动成功后，前端服务运行在 `http://localhost:5173`。

Vite 已配置代理，开发环境下 `/api` 开头的请求会自动转发到 `http://localhost:3000`。

---

## 5. 系统架构

### 5.1 整体架构

```
┌─────────────────────────────────────────────────────┐
│                    浏览器 (Browser)                    │
│                  Vue 3 SPA 应用                       │
└───────────────────────┬─────────────────────────────┘
                        │ HTTP / Axios
                        │ (开发环境通过 Vite Proxy 转发)
                        ▼
┌─────────────────────────────────────────────────────┐
│                 Express 后端服务                       │
│  ┌───────────┐  ┌────────────┐  ┌───────────────┐   │
│  │ Middleware │→ │   Router   │→ │  Controller   │   │
│  │ (CORS,    │  │ (RESTful   │  │ (业务逻辑处理)  │   │
│  │  Helmet,  │  │   路由)     │  │               │   │
│  │  JWT Auth)│  │            │  │               │   │
│  └───────────┘  └────────────┘  └───────┬───────┘   │
│                                         │            │
│                                         ▼            │
│                              ┌───────────────────┐   │
│                              │  Sequelize ORM    │   │
│                              │  (数据访问层)       │   │
│                              └─────────┬─────────┘   │
└────────────────────────────────────────┼─────────────┘
                                         │ tedious 驱动
                                         ▼
                              ┌───────────────────┐
                              │  SQL Server 数据库  │
                              │  (XYMES)          │
                              └───────────────────┘
```

### 5.2 前端架构

```
App.vue
 └── Router (vue-router)
      ├── /login          → Auth/Login.vue        (无需认证)
      ├── /register       → Auth/Register.vue     (无需认证)
      └── / (AppLayout)   → 需要认证的页面
           ├── AppHeader   (顶栏：用户信息、通知)
           ├── AppSidebar  (侧边栏菜单)
           └── <router-view>  (内容区)
                ├── Dashboard      仪表板
                ├── Ticket/*       工单管理
                ├── Material/List  物料管理
                ├── Product/List   产品管理
                ├── Plan/List      计划管理
                ├── Customer/List  客户管理
                ├── Supplier/List  供应商管理
                ├── Employee/List  员工管理
                ├── Schedule/List  班次管理
                ├── Group/List     小组管理
                ├── Workshop/List  车间管理
                ├── Productionline/List 产线管理
                └── User/Management 用户管理(需Admin)
```

**核心设计模式：**

- **API 层** (`src/api/`)：每个业务模块一个文件，封装 Axios 请求
- **视图层** (`src/views/`)：每个模块一个目录，包含 `List.vue` 等页面组件
- **状态管理** (`src/store/`)：使用 Pinia，仅在需要全局共享的场景使用（认证、通知）
- **路由守卫**：通过 `router.beforeEach` 实现认证拦截和权限校验

### 5.3 后端架构

采用经典的 MVC 分层架构：

```
请求 → Middleware → Router → Controller → Sequelize → SQL Server
                                ↓
                          Response (JSON)
```

**各层职责：**

| 层级 | 目录 | 职责 |
|------|------|------|
| 中间件 | `middleware/` | JWT 认证、角色权限、错误处理、文件上传 |
| 路由 | `routes/` | URL 与 Controller 方法的映射 |
| 控制器 | `controllers/` | 业务逻辑处理、请求参数校验、响应返回 |
| 模型 | `models/` | Sequelize ORM 模型定义、数据库表映射 |
| 工具 | `utils/` | JWT、密码哈希、统一响应格式等 |

### 5.4 认证与安全

- **JWT 认证**：用户登录后获取 Token，前端存储于 localStorage，每次请求通过 `Authorization: Bearer <token>` 携带
- **密码安全**：使用 bcryptjs 进行密码哈希，数据库不存储明文密码
- **安全中间件**：
  - `helmet`：设置安全相关 HTTP 头
  - `cors`：跨域资源共享控制
- **角色权限**：支持 `admin` / `user` 角色，部分路由（如用户管理）需 admin 权限

---

## 6. API 路由总览

所有 API 以 `/api` 为前缀。需认证的接口须在请求头中携带 `Authorization: Bearer <token>`。

| 模块 | 基础路径 | 支持操作 |
|------|----------|----------|
| 认证 | `/api/auth` | 登录、注册、获取当前用户、修改密码 |
| 仪表板 | `/api/dashboard` | 获取统计数据 |
| 工单 | `/api/tickets` | CRUD、评论、附件 |
| 用户管理 | `/api/users` | CRUD（需 admin） |
| 物料管理 | `/api/materials` | 查询、新增、修改、删除、导入、导出 |
| 产品管理 | `/api/products` | 查询、新增、修改、删除、导入、导出 |
| 计划管理 | `/api/plans` | 查询、新增、修改、删除、导入、导出 |
| 客户管理 | `/api/customers` | 查询、新增、修改、删除、导入、导出 |
| 供应商管理 | `/api/suppliers` | 查询、新增、修改、删除、导入、导出 |
| 员工管理 | `/api/employees` | 查询、新增、修改、删除、导入、导出 |
| 班次管理 | `/api/schedules` | 查询、新增、修改、删除、导入、导出 |
| 小组管理 | `/api/groups` | 查询、新增、修改、删除、导入、导出 |
| 车间管理 | `/api/workshops` | 查询、新增、修改、删除、导入、导出 |
| 产线管理 | `/api/productionlines` | 查询、新增、修改、删除、导入、导出 |

**标准 RESTful 接口格式（以物料管理为例）：**

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/materials?page=1&pageSize=20&search=xxx` | 分页查询 |
| POST | `/api/materials` | 新增 |
| PUT | `/api/materials/:id` | 修改 |
| DELETE | `/api/materials/:id` | 删除 |
| GET | `/api/materials/export` | 导出 CSV |
| POST | `/api/materials/import` | 导入 CSV |

**统一响应格式：**

```json
{
  "success": true,
  "data": { ... },
  "message": "操作成功"
}
```

---

## 7. 数据库表结构

系统使用 SQL Server 数据库 `XYMES`，包含以下业务表：

| 表名 | 说明 | 主键 |
|------|------|------|
| Users | 系统用户 | id (自增) |
| Tickets | 工单 | id (自增) |
| Comments | 工单评论 | id (自增) |
| Attachments | 附件 | id (自增) |
| Notifications | 通知 | id (自增) |
| material | 物料 | item_number |
| product | 产品 | product_id |
| Production_plan | 生产计划 | item_number |
| customer | 客户 | Customer_id |
| supplier | 供应商 | supplier_id |
| employee | 员工 | employee_number |
| schedules | 班次 | schedules_id |
| [group] | 小组 | group_id |
| workshop | 车间 | workshop_id |
| productionline | 产线 | productionline_id |

> 注意：`group` 是 SQL Server 保留字，在 SQL 查询中需要使用方括号 `[group]` 包裹。

---

## 8. 开发规范

### 8.1 前端开发规范

- **组件风格**：统一使用 `<script setup lang="ts">` 语法
- **状态管理**：局部状态使用 `ref`/`reactive`，全局共享使用 Pinia
- **API 调用**：所有接口调用封装在 `src/api/` 目录，使用统一的 Axios 实例
- **UI 组件**：统一使用 Ant Design Vue 组件库
- **分页默认值**：每页 20 条记录
- **日期处理**：统一使用 Day.js，格式为 `YYYY-MM-DD`

### 8.2 后端开发规范

- **控制器**：每个业务模块一个 controller 文件，导出具名函数
- **路由**：每个模块一个 routes 文件，在 `routes/index.ts` 统一注册
- **数据访问**：部分模块（基础数据类）直接使用 `sequelize.query()` 原生查询，工单等核心模块使用 Sequelize Model
- **错误处理**：Controller 内 try-catch 捕获异常，返回统一 JSON 格式
- **CSV 导入导出**：
  - 导出时添加 BOM 标记（`\uFEFF`）确保 Excel 正确识别中文编码
  - 导入时支持中文表头和英文字段名双重映射

### 8.3 新增业务模块步骤

1. **后端**：
   - 在 `controllers/` 下创建 `xxx.controller.ts`，实现 CRUD + 导入导出函数
   - 在 `routes/` 下创建 `xxx.routes.ts`，定义 RESTful 路由
   - 在 `routes/index.ts` 中注册新路由

2. **前端**：
   - 在 `api/` 下创建 `xxx.ts`，封装 API 请求函数
   - 在 `views/` 下创建 `Xxx/List.vue` 页面组件
   - 在 `router/index.ts` 中添加路由配置
   - 在 `components/Layout/AppSidebar.vue` 中添加菜单项

---

## 9. 构建与部署

### 9.1 前端构建

```bash
cd client
npm run build
```

构建产物输出到 `client/dist/` 目录，可部署到任意静态文件服务器（Nginx 等）。

### 9.2 后端构建

```bash
cd server
npm run build
```

TypeScript 编译输出到 `server/dist/` 目录，生产环境使用 `npm run start` 启动编译后的 JS 文件。

### 9.3 部署注意事项

- 前端部署时需配置 Nginx 反向代理，将 `/api` 路径转发到后端服务
- 确保 SQL Server 允许远程连接（如部署在不同服务器）
- 生产环境务必修改 `.env` 中的 `JWT_SECRET` 和数据库密码
- 建议使用 PM2 管理后端 Node.js 进程

**Nginx 配置示例：**

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态文件
    location / {
        root /path/to/client/dist;
        try_files $uri $uri/ /index.html;
    }

    # API 反向代理
    location /api {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 文件上传目录
    location /uploads {
        proxy_pass http://127.0.0.1:3000;
    }
}
```

---

## 10. 环境变量说明

后端环境变量配置文件：`server/.env`

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| PORT | 后端服务端口 | 3000 |
| DB_HOST | 数据库主机地址 | 127.0.0.1 |
| DB_PORT | 数据库端口 | 1433 |
| DB_NAME | 数据库名称 | XYMES |
| DB_USER | 数据库用户名 | sa |
| DB_PASSWORD | 数据库密码 | （需配置） |
| JWT_SECRET | JWT 签名密钥 | （需配置，建议随机生成） |
| JWT_EXPIRES_IN | JWT 过期时间 | 24h |
| UPLOAD_DIR | 文件上传目录 | uploads |
| MAX_FILE_SIZE | 最大上传文件大小（字节） | 10485760 (10MB) |
